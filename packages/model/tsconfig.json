{
  "$comment": "`./x.ts` import specifiers, as in packages/tokens: `rewriteRelativeImportExtensions` turns them into `./x.js` on emit, so dist/ is still ordinary ESM and Node can also read these sources directly with type stripping. Tests are excluded from the build and typechecked by tsconfig.tools.json instead, so the emitted package carries no test and no vitest import.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "tsBuildInfoFile": "dist/.tsbuildinfo",
    "lib": ["es2024"],
    "types": [],
    "allowImportingTsExtensions": true,
    "rewriteRelativeImportExtensions": true,
    "paths": {
      "@portolan/*": ["../../boundary/there-is-no-arrow-back-up.ts"]
    }
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
