// model — Graph types, wire form and identity keys, imported by both ends (AD-4, AD-5, AD-42).
//
// Empty by design. Story 1 fixes the build envelope and ships no product behaviour;
// the package exists so the dependency direction is enforced before anything imports it.

export {};
