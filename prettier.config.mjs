// Formatting. Indentation, charset, final newline and trailing whitespace come from
// .editorconfig, which Prettier reads and which this file deliberately does not restate.
// Only what .editorconfig cannot express is set here.
export default {
  singleQuote: true,
  printWidth: 100,
};
