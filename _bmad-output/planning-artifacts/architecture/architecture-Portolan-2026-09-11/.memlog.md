---
scope: Portolan v1 — read-only Docker Swarm topology map, whole product
purpose: build-substrate
altitude: feature
updated: 2026-09-14T13:18
---

- (direction) Voie coaching choisie par l'utilisateur (cohérent avec brief et PRD). Livrables: spine seule + deck HTML de présentation (ambition publique du projet).
- (event) Pull effectué avant démarrage: origin/main portait le PRD final (83 FR/20 NFR) et les spines UX (DESIGN.md, EXPERIENCE.md) absents du worktree. Entrées de tête: prd.md (autorité sur le QUOI), DESIGN.md + EXPERIENCE.md (gouvernent le détail), brief.md + addendum.md.
- (constraint) NFR-5 [ADOPTED, hérité du brief 2026-09-09]: trois coutures collecteur/modèle/rendu; modèle de graphe et renderer indépendants du collecteur Swarm. Un second source-of-truth = un nouveau collecteur, pas une réécriture.
- (constraint) NFR-6 [ADOPTED]: le couplage UI↔collecteur est limité à exactement 3 surfaces — vocabulaire Docker des objets (FR-80), écran socket-injoignable (FR-57), message engine-trop-vieux (FR-64).
- (constraint) NFR-1 [ADOPTED]: une seule image conteneur, un docker stack deploy, tourne dans le swarm qu'elle cartographie, un swarm par instance.
- (constraint) NFR-4 [ADOPTED]: air-gapped par construction — aucun CDN, aucune police externe, aucun asset externe. Tout est servi par Portolan, polices comprises.
- (constraint) NFR-8 [ADOPTED]: la latence d'interaction EST l'exigence de perf, pas un plancher de framerate. 100ms pour pan/zoom/sélection/filtre/recherche à l'échelle NFR-7 (396 objets) sur GPU intégré. Le mouvement continu est ce qui cède.
- (constraint) NFR-17 [ADOPTED]: AGPLv3. Contraint le choix des dépendances — toute dépendance doit être compatible AGPLv3.
- (question) Les 6 questions portées à l'architecture par le PRD §7.3, dans son ordre: (1) lisibilité par défaut du landing frame §7.1, (2) dimensionnement des marques §7.2/NFR-9, (3) exposition réseau NFR-2, (4) proxy socket manager NFR-3, (5) clamp de luminance NFR-9, (6) stack front + lib de rendu NFR-19.
- (direction) Position prise d'entrée et annoncée à l'utilisateur: (1) et (5) ne sont pas des décisions mais des MESURES (le PRD l'admet: 'répondre demande un vrai cluster de l'ordre NFR-7'). L'architecture décide ce qui rend la mesure possible — harnais de rendu, cluster synthétique à 396 objets, budget vérifiable — pas le verdict de lisibilité.
- (decision) Le SERVEUR NE DETIENT PAS LES POSITIONS. Layout côté client, en mémoire de l'onglet. Binds: FR-16, NFR-2. Prevents: la divergence où un build met l'état de layout côté serveur et crée une carte partagée que le produit n'a jamais demandée. Utilisateur: 'deux personnes peuvent avoir deux vues differentes'.
- (decision) LAYOUT DETERMINISTE: mêmes objets en entrée -> mêmes positions en sortie. La divergence entre deux vues vient de l'ACTION (filtre, isolate, Reorganise, mode de zone), jamais de l'ouverture. Binds: FR-16, FR-13, critère de réussite #1 (capture d'un inconnu). Prevents: un F5 qui refait le layout = 4e relayout non déclaré violant FR-16; une capture d'écran non reproductible. Coût accepté: interdit toute dépendance à l'horloge ou au timing des frames dans le layout (itérations fixes, graine fixe, ordre d'itération stable).
- (decision) LANGAGE: Node.js 24 LTS + TypeScript, serveur et client. Binds: NFR-5, NFR-6, NFR-1. Raison retenue: une seule définition du modèle de graphe, importée des deux côtés de la couture NFR-5, et le contrat NFR-6 (3 surfaces de couplage UI/collecteur) exécutable dans un seul langage. Go écarté explicitement: sa perf n'adresse rien — NFR-8 est entièrement côté navigateur, le serveur est I/O-bound sur la socket. Coût accepté et nommé: image ~80Mo (node:24-alpine) contre ~15Mo (Go scratch).
- (version) Vérifié par recherche web 2026-09-11: Node.js 24 = Active LTS (maintenance 2026-10-20, EOL 2028-04-30), Node 26 devient Active LTS 2026-10-28, Node 22 en maintenance (EOL 2027-04). dockerode 5.0.1 publiée ~juillet 2026, vivante. Docker Engine série 29 documentée. graphology 0.26.0 MIT mais dernière publication il y a ~2 ans - staleness à surveiller si retenue.
- (constraint) CONSTAT DE RECHERCHE: aucune bibliothèque de graphe (Sigma, Cytoscape, vis-network) ne sait rendre Portolan. FR-13 coques béziers semées, FR-70 étirement, FR-71 respiration en déformation locale, FR-40 champs de teinte composités + isolignes, FR-82 contour de stack dérivé, FR-66 rail de marques en espace écran — aucun n'est un point d'extension de ces libs. Le renderer est sur mesure; une lib ne peut servir que de modèle et/ou de layout.
- (decision) UNE DESCRIPTION DE SCENE, PLUSIEURS RASTERISEURS. Le renderer produit une scène indépendante de la résolution (zones=champs, corps=données de chemin, marques ancrées en espace écran, texte avec son plancher); un backend écran la peint, un sérialiseur SVG l'écrit. Binds: FR-46, FR-47, FR-48, NFR-8, NFR-9, §7.1. Prevents: l'écran et l'export qui divergent (le shader que le SVG ne sait pas reproduire); et le pari irréversible Canvas2D vs WebGL pris avant toute mesure. Effet de bord majeur: §7.1 devient mesurable sans navigateur ni GPU — longueur des stubs, taille des corps, contraste sur pire champ composité deviennent des assertions de test sur une scène générée à 396 objets synthétiques.
- (direction) Le backend écran (Canvas2D vs WebGL) est explicitement DIFFERE et rendu réversible par la décision de scène. À trancher sur mesure, pas sur pari. Argument décisif déjà identifié contre WebGL pur: texte à 9px via atlas SDF contre planchers NFR-10 (9px) et NFR-11 (7:1) dans un produit dont la thèse est la lisibilité.
- (decision) IDENTITE D'OBJET PAR SLOT, pas par ID Docker. Clé: service répliqué = stack/service/slot; service global = stack/service/noeud; volumes et réseaux = nom; noeuds et services = ID Docker (stables, un stack deploy met à jour les services sans les recréer). Binds: FR-16, FR-13, FR-71. Prevents: qu'un docker stack deploy rebatte positions ET silhouettes de toute la stack — réintroduisant dans le produit le défaut exact de la scène fondatrice ('un dessin obsolète au prochain stack deploy'). Fondement vérifié: dans Swarm une tâche est immuable, une mise à jour de service détruit et recrée ses tâches (nouveaux ID de conteneur), mais le numéro de slot survit.
- (decision) ECART DECLARE PAR RAPPORT A FR-13: la silhouette est semée depuis la CLE D'IDENTITE (slot), pas depuis l'ID Docker du conteneur comme FR-13 l'écrit. Raison: semée depuis l'ID conteneur, la promesse 'the same shape across every survey' est fausse dès le premier redéploiement — le canal de reconnaissance se réinitialise au moment précis où l'utilisateur regarde la carte pour comprendre ce qui a bougé. A REMONTER AU PRD.
- (constraint) COUT ACCEPTE, nommé à l'utilisateur et retenu: l'identité par slot rend deux conteneurs réellement différents (avant/après redéploiement, image possiblement différente, noeud possiblement différent) comme UN MEME CORPS. La carte affirme une continuité que Docker ne connaît pas. Contrepartie retenue: le slot EST l'identité de l'opérateur (docker service ps affiche web.1), et l'ID du conteneur reste visible dans le panneau de détail (FR-25) où il ne trompe personne.
- (decision) SOCKET DOCKER MONTEE EN DIRECT, pas de proxy sidecar. Tranché par l'utilisateur CONTRE le penchant de l'assistant (qui recommandait un proxy à liste blanche de 7 endpoints). Conséquence assumée: NFR-3 reste une promesse tenue par le code, pas un mécanisme externe. Bénéfice: NFR-1 ('une seule image conteneur') tient sans amendement, et le premier lancement n'exige aucune pièce supplémentaire. Cohérent avec FR-2, qui dit déjà que le chrome affiche READ ONLY sans revendiquer de mécanisme.
- (decision) EXPOSITION PAR DEFAUT: mode host sur 127.0.0.1, plus contrainte de placement node.role == manager. Le stack file publié est COMMENTE avec les trois façons de l'ouvrir (routing mesh, overlay interne + reverse proxy, VPN) juste au-dessus de la ligne à décommenter. Binds: NFR-2, NFR-1. Prevents: le v1 contre lequel le brief mettait en garde — un visualiseur de topologie complète non authentifié avec export en un clic, joignable depuis n'importe quel noeud.
- (decision) NOUVELLE EXIGENCE, sans amont: un écran 'non joignable' symétrique de FR-57. Un défaut sûr sans écran qui l'explique est un produit qui échoue en silence au premier contact — et FR-57 n'a aucun équivalent pour l'exposition. A REMONTER AU PRD comme FR nouveau.
- (decision) PARADIGME: pipeline unidirectionnel, adaptateurs aux deux bouts. Collecteur Docker -> Modele -> Layout -> Scene -> Rasteriseurs (ecran, SVG). FR-2 rend l'absence de chemin retour reelle: aucun cote commande nulle part.
- (decision) UN SEUL ETAGE STATEFUL: le layout. Tous les autres etages sont des fonctions pures de leur entree. Prevents: de l'etat de survey disperse dans plusieurs etages, et repond a 'ou chercher quand la carte bouge alors qu'elle ne devrait pas'.
- (decision) ETAT DE VUE ET ETAT DE LAYOUT = DEUX MAGASINS SEPARES; l'etat de vue n'a aucun chemin d'ecriture vers le layout. Seules les 3 actions nommees par FR-16 (Reorganise, changement de mode de zone, bascule du fond de noeuds) appellent le layout. Binds: FR-16, FR-38, FR-79. Prevents: qu'un filtre, une selection, une recherche ou l'ouverture du panneau deplace quoi que ce soit.
- (decision) TRANSPORT: SSE, instantane complet a chaque sondage, pas de deltas. Binds: FR-3, FR-54, FR-55. Prevents: un protocole de reconciliation par deltas paye sans gain mesure a 400 objets; et la divergence poll-vs-push entre deux builds. Staleness derivee de l'horodatage du sondage dans la charge + deconnexion SSE.
- (decision) LAYOUT = FONCTION PURE (modele, graine, mode) -> positions. Iterations fixes, ordre d'iteration stable, aucune dependance a l'horloge ni au timing des frames, reservation de la coque deformee AVANT placement (FR-13). La bibliotheque de layout est du SEED, pas de la spine.
- (decision) MODELE = UN PAQUET DE TYPES TYPESCRIPT importe par le collecteur et par le renderer. Les 3 surfaces de NFR-6 (vocabulaire Docker FR-80, ecran socket-injoignable FR-57, message engine-trop-vieux FR-64) sont les SEULS endroits ou le renderer a le droit de connaitre un docker-isme.
- (decision) ADAPTATEUR DOCKER EN LECTURE SEULE PAR CONSTRUCTION: le client HTTP sous-jacent refuse toute methode autre que GET. Ajoute A L'INTERIEUR de la decision 'socket directe' de l'utilisateur, sans la rouvrir. Transforme 'on n'a ecrit aucune ecriture' en 'on ne peut pas en ecrire'. Proxy a liste blanche documente en durcissement OPTIONNEL dans le README.
- (decision) i18n: chaines externalisees des le premier commit, un catalogue par langue, aucun litteral de chaine dans un composant. NFR-18.
- (question) §7.2 (dimensionnement des marques au niveau d'atterrissage) DIFFERE JUSQU'A MESURE, accord explicite de l'utilisateur. Condition de revisite: la sortie du harnais de mesure sur cluster synthetique a 396 objets, canvas operatif 884px. Proprietaire de la resolution: produit/design, pas architecture — choisir laquelle des 4 exigences cede coute au produit, pas au code. L'utilisateur, interroge sur son penchant, repond honnetement 'je ne sais pas' — AUCUNE direction inventee ici.
- (decision) HARNAIS DE MESURE = LIVRABLE DE V1, pas outil de confort. Genere un cluster synthetique a l'echelle NFR-7 (396 objets), produit la scene sur le canvas operatif de 884px (largeur reelle une fois la colonne panneau reservee par FR-79 — et NON les 1204px sur lesquels le PRD a calcule), et rapporte: distribution du diametre rendu des corps, occupation du rail de marques en fraction du corps, distribution de la longueur des stubs, contraste des deux types d'aretes sur le pire champ composite. Binds: §7.1, §7.2, NFR-7, NFR-9, NFR-10, NFR-11. Il ne FIXE aucun seuil — le PRD refuse deliberement d'en inventer et l'architecture ne le fera pas non plus.
- (direction) DEMANDE EXPLICITE DE L'UTILISATEUR, non encore traitee: 'j'aimerais clairement mettre en place une CI et des tests'. Dimension verification/CI A COUVRIR A LA REPRISE — strategie de test, harnais de mesure (deja decide comme livrable v1) branche dans la CI, build multi-arch, publication des releases. C'est la seule dimension structurelle volontairement laissee ouverte a ce checkpoint.
- (direction) PROPOSE MAIS NON RATIFIE (l'utilisateur a interrompu pour checkpointer avant de repondre) — enveloppe operationnelle en 6 points: (1) Portolan n'ecrit rien: aucun volume, aucune base, aucun cache disque, conteneur jetable; (2) le serveur garde le dernier bon sondage et le rejoue immediatement a un nouvel onglet, sinon attente jusqu'a 60s devant du vide; (3) sondage en echec: le serveur continue de servir le dernier bon sondage avec son age, pas du vide — c'est ce qui rend FR-54 possible cote client; (4) image multi-arch amd64 + arm64, la tribu homelab est largement sur ARM; (5) configuration par variables d'environnement uniquement, l'intervalle de rafraichissement n'en fait pas partie (FR-3 en fait un controle d'interface); (6) le healthcheck teste que Portolan SERT, pas que le sondage reussit — un echec de sondage est un etat produit (FR-54), pas un conteneur malade, sinon Swarm redemarre Portolan a chaque hoquet de socket et la carte en cours de lecture disparait. A RATIFIER A LA REPRISE.
- (event) CHECKPOINT: run interrompu par l'utilisateur avant distillation. ARCHITECTURE-SPINE.md N'EST PAS ECRIT. Aucune passe de Reviewer Gate, aucune reconciliation d'entrees, aucun deck. Toutes les decisions structurantes vivent dans ce memlog et nulle part ailleurs. REPRISE: relire ce memlog, ratifier le bloc enveloppe operationnelle, traiter CI + tests, puis Finalize (distiller, reconcilier les entrees, Reviewer Gate, triage, deck HTML).
- (event) REPRISE 2026-09-14. Memlog rechargé comme acquis, voie coaching reconduite. Deux blocs ouverts: enveloppe opérationnelle (à ratifier) et CI+tests. Dépôt vérifié: greenfield total, aucun code, GitHub LeRoiDangmar/Portolan.
- (decision) ENVELOPPE OPERATIONNELLE RATIFIEE (4 points sur 6 tels que proposés): (1) Portolan n'écrit rien côté serveur — aucun volume, aucune base, aucun cache disque, conteneur jetable; (3) sondage en échec = le serveur continue de servir le dernier bon sondage avec son âge, jamais du vide — c'est ce qui rend FR-54 réalisable côté client; (4) image multi-arch amd64 + arm64 (node:24-alpine couvre les deux), la tribu homelab est largement sur ARM; (6) le healthcheck teste que Portolan SERT, pas que le sondage réussit. Binds: NFR-1, FR-54, FR-57. Prevents: qu'un build fasse du sondage un critère de santé conteneur — Swarm redémarrerait Portolan à chaque hoquet de socket et la carte en cours de lecture disparaîtrait.
- (decision) CADENCE DE SONDAGE ET CADENCE DE RENDU SONT DECOUPLEES. Le serveur tient UNE boucle de sondage, à la cadence minimale parmi les clients connectés; chaque onglet n'affiche un nouvel instantané qu'à SA cadence sélectionnée. Binds: FR-3, FR-5, NFR-3. Prevents: la divergence non tranchée où deux onglets choisissent deux intervalles différents sur une boucle unique — sans cette règle un build fait de l'intervalle une propriété serveur (le dernier onglet qui parle change la carte des autres, violant 'altère ni la population ni le rendu'), un autre ouvre une boucle de sondage par connexion (N onglets = N fois la charge sur la socket manager, et des sondages divergents entre onglets). Conséquence: un onglet seul à 60s sonde à 60s; deux onglets (5s, 60s) sondent à 5s et le 60s ne bouge toujours que toutes les 60s. FR-5 tient: l'horodatage affiché est celui de l'instantané rendu, pas celui du dernier sondage reçu.
- (decision) PREFERENCES D'AFFICHAGE = localStorage DU NAVIGATEUR, jamais côté serveur. Couvre palette (FR-43), clair/sombre (FR-44), taille de texte (FR-62), masquage écran (FR-51), mode de zone, fond de nœuds, intervalle de rafraîchissement (FR-3). Binds: NFR-1, FR-3, FR-43, FR-44, FR-51, FR-62. Prevents: qu'un build introduise un volume ou une base pour mémoriser des préférences, cassant 'conteneur jetable' et NFR-1; et qu'un autre ne mémorise rien, obligeant la persona exécutif à reposer palette et masquage à chaque ouverture. Coût nommé et retenu: la carte que tu ouvres n'est plus forcément celle que ton collègue ouvre — mais l'AD layout l'admettait déjà (la divergence vient de l'action). Note de cohérence: mode de zone et fond de nœuds sont deux des 3 actions de relayout FR-16; restaurés au chargement ils entrent comme paramètre d'entrée du layout pur, pas comme un 4e relayout.
- (decision) CORRECTION du point (2) proposé au checkpoint, qui était FAUX ('attente jusqu'à 60s devant du vide'). Ouverture d'un onglet, trois branches exhaustives: dernier bon sondage existe -> rejeu immédiat; aucun sondage bon ET dernière tentative en échec -> écran FR-57 TOUT DE SUITE, sans minuteur; aucun sondage bon ET aucune tentative achevée -> chargement à froid FR-53. Binds: FR-53, FR-57. Prevents: 60s de produit mort au premier contact — FR-57 est le seul écran du produit autorisé à enseigner et il porte la ligne de configuration qui corrige le montage de socket; le retarder derrière un minuteur le rend inutile au moment exact où il sert.
- (decision) CONFIGURATION PAR VARIABLES D'ENVIRONNEMENT UNIQUEMENT (point (5) ratifié, précisé par la décision localStorage). Aucun fichier de configuration, aucun argument de ligne de commande. L'intervalle de rafraîchissement n'est PAS une variable d'environnement: FR-3 en fait un contrôle d'interface et la décision localStorage en fait une préférence de navigateur. Frontière: les variables d'environnement portent ce qui appartient au déploiement (adresse d'écoute, chemin de socket, niveau de log, langue par défaut NFR-18); le navigateur porte ce qui appartient à la lecture de la carte. Prevents: un build qui double une préférence d'affichage en variable d'environnement et crée deux sources de vérité pour le même réglage.
- (decision) ORDRE TOTAL IMPOSE PAR LE COLLECTEUR, avant le modèle. TROU TROUVE A LA REPRISE en travaillant le test de déterminisme: l'AD layout exige un 'ordre d'itération stable' mais ne dit jamais d'où vient l'ordre, et l'API Docker ne garantit AUCUN ordre sur ses list. Règle: le collecteur trie chaque collection par sa clé d'identité (celle de l'AD identité-par-slot) avant de la remettre au modèle; aucun étage aval ne dépend de l'ordre de réponse de Docker. Binds: FR-16, FR-13, FR-1. Prevents: la divergence où deux unités sont chacune parfaitement conformes — le collecteur rend ce que Docker lui a donné, le layout itère dans l'ordre d'entrée — et où les positions se rebattent entre deux sondages d'un cluster INCHANGE. C'est la violation de FR-16 la plus grave possible, et le test de déterminisme ne l'attrape pas: il rejoue la même fixture dans le même ordre.
- (decision) LA SCENE EST LA SURFACE D'ASSERTION, PAS LES PIXELS. Lisibilité, contraste, géométrie et planchers se testent sur la description de scène, jamais par comparaison d'images. Aucun test de régression visuelle comme garde de correction. Binds: §7.1, NFR-10, NFR-11, NFR-13, FR-67, FR-68. Prevents: qu'un build installe une suite de captures d'écran — fragile au GPU, au rendu de police et à la version de navigateur — qui sera désactivée en un mois, laissant les planchers de lisibilité sans aucune garde alors qu'ils sont la thèse du produit. C'est l'effet de bord majeur déjà nommé par la décision scène/rasteriseurs, ici érigé en règle de vérification.
- (decision) DEUX SORTES DE FIXTURES, JAMAIS MELANGEES. (a) Charges d'API Docker ENREGISTREES sur un vrai cluster: la seule vérité admissible pour tester le collecteur et l'adaptateur. (b) Cluster SYNTHETIQUE GENERE à l'échelle NFR-7 (396 objets): la seule entrée admissible pour les tests d'échelle, de lisibilité et le harnais de mesure. Un test de collecteur ne tourne jamais sur du synthétique; un test d'échelle ne tourne jamais sur un enregistrement. Binds: FR-1, FR-64, NFR-7, NFR-5. Prevents: que quelqu'un génère des charges Docker plausibles et que le collecteur se retrouve testé contre l'idée qu'on se fait de l'API plutôt que contre ce que Docker répond vraiment — précisément là où FR-64 dit que le comportement de l'engine varie selon les versions.
- (decision) DETERMINISME DU LAYOUT = TEST EXECUTABLE, BIT-A-BIT, SUR LES DEUX ARCHITECTURES. Même (modèle, graine, mode) -> positions bit-identiques, dans le même processus, entre deux processus, et entre amd64 et arm64. Interdits nommés dans l'étage layout: Math.random, toute lecture d'horloge, tout tri sans comparateur d'ordre total, toute dépendance au timing des frames. Binds: FR-16, FR-13, critère de réussite #1. Prevents: qu'un build introduise une source de variation (itération sur un Set construit dans l'ordre de réponse Docker, tri instable, jitter aléatoire de placement) que rien ne détecte avant qu'un utilisateur signale une carte qui bouge sans raison.
- (version) Vérifié par recherche web 2026-09-14. (1) Runners GitHub Actions arm64 hébergés: GA et GRATUITS sur dépôt public depuis août 2025 (labels ubuntu-24.04-arm, ubuntu-22.04-arm, 4 vCPU); disponibles en dépôt privé depuis janvier 2026. Le test de déterminisme cross-arch est donc exécutable sans coût ni runner auto-hébergé. (2) node:test stable depuis Node 20, production-ready en Node 24, avec assertions, mocks, couverture, watch et exécution parallèle intégrés — zéro dépendance de test. (3) Déterminisme flottant: V8 embarque sa propre fdlibm statiquement liée, identique sur tout OS, et la sémantique JS exige un arrondi indépendant par opération (un moteur conforme n'a pas le droit de fusionner en FMA). Le bit-à-bit cross-arch est donc quasi gratuit en JS — ce qui n'aurait été vrai ni en Go ni en Rust. Bénéfice non anticipé de la décision Node+TypeScript.
- (decision) LES TOKENS DE DESIGN SONT UN ARTEFACT MACHINE DANS LE DEPOT, ET IL EST LA SOURCE DE VERITE. Un seul fichier (les 142 tokens de couleur, plus géométrie, type et espacement); le CSS/le renderer et les tests de contraste importent EXACTEMENT le même fichier. Aucune valeur de couleur littérale nulle part ailleurs dans le code. Binds: NFR-11, NFR-12, NFR-13, NFR-14, FR-43, FR-44. Prevents: la divergence où DESIGN.md reste normatif en prose, quelqu'un recopie les valeurs à la main dans le CSS, et les tests de contraste valident la recopie au lieu du design — un écart de recopie devient alors invisible et permanent. Coût nommé et accepté par l'utilisateur: DESIGN.md cesse d'être normatif sur les VALEURS et devient la documentation de l'intention (le pourquoi de chaque token, le registre, les figures mesurées). A REMONTER A LA SPINE UX.
- (decision) NFR-11 ET NFR-13 SONT DES GATES CI BLOQUANTS DES LE PREMIER COMMIT. Les contrastes (7:1 canal identifiant, 4.5:1 châssis, 4:1 santé, 3:1 les deux sortes d'arêtes sur le pire champ composité, 3:1 anneau de focus) et la séparabilité mutuelle des six teintes de zone sous deutéranopie simulée dans LES DEUX palettes sont calculés depuis le fichier de tokens et bloquent la fusion. Les trois états exemptés par NFR-11 (voile de péremption FR-54, atténuation d'accessibilité FR-33, pâle de filtre vide FR-34) sont encodés comme exemptions NOMMEES dans le test — un test qui échouerait sur un comportement correct serait désactivé. Binds: NFR-11, NFR-13, NFR-12. Prevents: que les planchers de lisibilité restent des promesses dans un document pendant que le code s'en écarte silencieusement, sur un produit dont la thèse EST la lisibilité.
- (constraint) CONSEQUENCE ASSUMEE DU GATE NFR-13, choisie sciemment par l'utilisateur contre les deux options plus douces: LE BUILD DEMARRE ROUGE. Deux teintes de la palette CLAIRE simulent aujourd'hui à une valeur byte-identique sous deutéranopie — le PRD le qualifie de défaut, pas de résidu acceptable, et d'autant plus que la palette claire est celle qui tient le mieux le cadre dense (FR-75). NFR-13 nomme déjà son propriétaire: DESIGN, avant implémentation de la palette. Le gate ne fait que rendre cette échéance inéluctable au lieu de la laisser glisser. Condition de sortie du rouge: correction de la palette claire par design.
- (decision) LE HARNAIS DE MESURE EST UN CLIQUET ANTI-REGRESSION EN CI, SANS SEUIL ABSOLU. Il tourne à chaque PR sur le cluster synthétique à 396 objets et le canvas opératif de 884px, et bloque si l'une de ses mesures EMPIRE par rapport à main: distribution du diamètre rendu des corps, occupation du rail de marques en fraction du corps, distribution de la longueur des stubs, contraste des deux types d'arêtes sur le pire champ composité. Binds: §7.1, §7.2, NFR-7, NFR-9, NFR-10, NFR-11. Prevents: à la fois l'érosion silencieuse de la lisibilité commit après commit, ET l'invention d'un seuil que le PRD refuse explicitement de fixer (§7.1, §7.2). Un cliquet est un gate sans seuil: il ne dit pas ce qui est bon, il interdit d'aller vers le pire. La référence du cliquet se met à jour à chaque fusion sur main.
- (decision) TESTS NAVIGATEUR = FUMEE ET ACCESSIBILITE DU CHASSIS UNIQUEMENT. Portée close et nommée: le serveur démarre et sert, le flux SSE s'établit et délivre un instantané, la carte se dessine, les contrôles de châssis sont tab-focusables dans l'ordre (FR-29, FR-76, FR-77), l'écran FR-57 s'affiche sans socket, le message FR-64 s'affiche sur engine trop vieux. AUCUN parcours utilisateur, AUCUNE assertion de lisibilité ou de géométrie dans le navigateur. Binds: FR-29, FR-57, FR-64, FR-76, FR-77. Prevents: la suite E2E lente et fragile qui est le mode d'échec classique d'un produit graphique — et le double emploi avec les assertions de scène, qui couvrent déjà lisibilité et géométrie sans navigateur. Les quatre parcours d'EXPERIENCE.md sont explicitement HORS portée automatisée.
- (decision) CE QUE LA CI NE PEUT PAS GARDER, DIT PLUTOT QUE SIMULE: NFR-8 (100ms à 396 objets sur GPU intégré) N'EST PAS un gate CI. Un runner hébergé partagé sans GPU ne mesure pas cela, et un gate de perf fragile finit désactivé. Ce que la CI garde à sa place: le temps de génération de la SCENE à 396 objets, qui est CPU-bound, mesurable et honnête, sous forme de cliquet. NFR-8 lui-même se mesure à la main, sur la machine de l'auteur, avec le harnais. Binds: NFR-8, NFR-7. Prevents: qu'un build installe un gate de latence sur runner partagé, le voie clignoter, et le désactive — laissant croire que NFR-8 est gardé alors qu'il ne l'est plus.
- (decision) CI ET RELEASE: GitHub Actions (le dépôt est sur GitHub). Sur PR et sur main: lint, typecheck, tests unitaires et de scène, gates NFR-11/NFR-13, cliquets du harnais, tests de fumée navigateur, et le test de déterminisme exécuté sur ubuntu-24.04 ET ubuntu-24.04-arm. main construit l'image sans la publier. La publication est déclenchée par un tag v*, et pousse l'image multi-arch amd64+arm64 sur GHCR ET Docker Hub — GHCR parce qu'il ne demande aucun secret supplémentaire sur un dépôt public, Docker Hub parce que c'est là que la tribu homelab cherche. Binds: NFR-1, NFR-17. Prevents: la divergence sur le déclencheur de publication (chaque push sur main publiant une image que personne n'a décidé de sortir).
- (decision) STACK FRONT (seconde moitié de NFR-19): REACT, choisi par l'utilisateur. L'assistant recommandait Svelte 5 (compilé, quasi aucun runtime, meilleur pour NFR-4/NFR-1); l'utilisateur a tranché pour React et c'est sa décision. Arguments retenus en sa faveur: c'est l'écosystème sur lequel il trouvera de l'aide, et le châssis de Portolan est petit. Coût nommé et accepté: le plus gros runtime des options envisagées, dans un produit air-gapped où tout est servi depuis l'image, et pour un châssis qui n'utilise aucune des raisons d'être de React (pas de routage, pas d'état serveur, aucun écosystème de composants tiers — NFR-4 les interdit de fait). Périmètre STRICTEMENT limité par la décision de confinement qui suit.
- (decision) CONFINEMENT DU FRAMEWORK: React ne franchit JAMAIS la surface de carte. Aucun objet de graphe — corps, zone, arête, marque, contour de stack — n'est un composant React, un élément de liste réconcilié, ni un nœud d'un arbre React. La carte est une seule surface montée une fois, peinte depuis la description de scène par le rasteriseur écran; React possède exclusivement le châssis (barre d'outils FR-76, panneau de détail FR-25/FR-79, bandeau de légende FR-78, filtres, recherche FR-36, contrôles d'affichage, écrans FR-57 et FR-60). Le seul trafic à travers la couture est l'état de vue: React écrit dans le magasin d'état de vue, le rasteriseur le lit. Binds: NFR-8, NFR-19, FR-79. Prevents: la divergence qui tue le produit — un build qui rend 396 corps par un système de composants réactif, faisant passer chaque survol, chaque sélection et chaque frame de respiration par le réconciliateur. Cette règle devient PLUS load-bearing avec React qu'elle ne l'aurait été avec Svelte, et c'est le prix de la décision précédente.
- (version) Vérifié par recherche web 2026-09-14: React 19.3 stable, publiée le 9 septembre 2026; pas de React 20. React Compiler stable en 1.0 depuis octobre 2025. Svelte 5.56.x (écarté par choix utilisateur). Note: le React Compiler est optionnel ici — le châssis est petit et le chemin chaud (la carte) est hors React par construction.
- (event) RECONCILIATION DES ENTREES: 2 sous-agents (PRD; DESIGN+EXPERIENCE). Lint déterministe 0 finding avant et après. Constat majeur: la première distillation avait sauté §3.3-§3.7 du PRD. 5 AD nouveaux (AD-33..AD-37) et 14 AD amendés en place. Rapports: reviews/reconcile-prd.md, reviews/reconcile-ux.md.
- (decision) AD-33 CHAQUE VUE EST SA PROPRE SURFACE, avec son propre layout, clé (vue, sujet, mode, graine). Changer de vue ou de sujet DESSINE une autre surface, ne relaye jamais celle qu'on quitte, et revenir restaure ses positions. La règle des 3 actions d'AD-3 est portée au périmètre d'UNE surface. Node view: la proportionnalité des régions (FR-73) et son plancher (FR-59) sont un MODE DE LAYOUT, pas un traitement de rendu — la proportionnalité décide le placement. Service view: un SOUS-ENSEMBLE DU MODELE atteint le layout (FR-20), le zoom sémantique est en amont du placement et jamais une transformation de caméra. Binds: FR-17..FR-21, FR-59, FR-73, FR-16, FR-41. Prevents: que les trois vues n'aient aucune existence architecturale — un builder obéissant littéralement à AD-3 n'a AUCUNE route légale vers le layout pour un changement d'onglet, alors que FR-16 réserve explicitement le cas.
- (decision) AD-34 LE MASQUAGE EST UN PARAMETRE DE LA SCENE, pas un comportement de rasteriseur. Ecran et export construisent chacun leur scène avec leur propre drapeau, indépendants dans les deux sens. Binds: FR-47, FR-49, FR-50, FR-51, FR-52. Prevents: DEUX échecs à la fois — l'unique contrôle de confidentialité du produit gouverné par rien, ET la collision frontale avec AD-9 (aucun rasteriseur ne détient ce que l'autre ne peut obtenir de la scène) alors que l'exception DECLAREE ET PAR DEFAUT de FR-47 est exactement cela. Bénéfice: 'une valeur masquée garde une empreinte identique' (FR-49) devient une propriété de la scène, donc une assertion qu'AD-26 sait faire.
- (decision) AD-35 LE MOBILIER DE CARTE ET TOUTE FIGURE DERIVEE DU CHART VIENNENT DE LA SCENE. La scène porte graduations du biseau, marques de calage, et LE CONTENU DE LA LEGENDE (quels réseaux sont réellement sur le chart, teintes, motifs d'octave, troncature — FR-63). React rend le bandeau écran depuis cette description (FR-78); le sérialiseur SVG écrit la même description dans l'export (FR-48). Aucun des deux ne la calcule. Même règle pour les extents de Fit to chart (FR-76), le compteur d'orphelins (FR-35) et l'ensemble allumé par la recherche (FR-36, FR-37). Prevents: FR-48 littéralement inconstructible — AD-10 donnait la légende à React en exclusivité pendant qu'AD-9 interdit au sérialiseur SVG d'inventer ce que la scène ne porte pas.
- (decision) AD-36 LE PICKING EST LE SEUL CHEMIN MONTANT DE LA COUTURE DE CARTE, ET IL PORTE DES CLES D'IDENTITE. La géométrie de hit vit dans la scène; la surface résout la position du pointeur et n'émet QU'UNE CLE D'IDENTITE (AD-5) vers view-state. Jamais un objet de scène, jamais une géométrie, jamais un nœud DOM. L'ensemble sélectionnable de FR-28 est une propriété de la géométrie de pick de la scène. Binds: FR-22, FR-26, FR-27, FR-28. Prevents: qu'AD-10 déclare la couture à sens unique alors que survol et clic naissent manifestement sur la surface de carte — laissant le hit-testing sans propriétaire et invitant un couplage du châssis au renderer par la porte de derrière. Effet gratuit: FR-26 ('sélectionner une copie sélectionne l'objet') tombe tout seul, les deux copies portent la même clé.
- (decision) AD-37 LA STABILITE EST UNE SECONDE PROPRIETE, DISTINCTE DU DETERMINISME, ET TESTEE A PART. Déterminisme = même entrée -> même sortie. Stabilité = entrée CHANGEE -> sortie presque inchangée. Aucun test de déterminisme ne peut la voir: il ne change jamais l'entrée. Règle: les survivants ne bougent pas, un nouvel objet est placé près de ses voisins dans de l'espace libre, la cellule d'un disparu est RETENUE (il s'efface sur place) et son espace n'est pas récupéré avant le prochain relayout. Testé en rejouant une séquence scriptée de sondages (ajout, retrait, scale up, scale down, redéploiement de stack). Binds: FR-16, FR-13, FR-71. Le cas tranchant est le redéploiement: tous les conteneurs ont un nouvel ID et le même slot, donc un build correct ne bouge RIEN — c'est là qu'AD-5 et AD-6 se paient.
- (decision) AMENDEMENTS EN PLACE issus de la réconciliation, ID conservés. AD-2: deux sortes d'état seulement — état DERIVE DU SONDAGE (layout) et état de PRESENTATION local à la frame (rasteriseur écran: phase de respiration, tween de relayout tenant les deux jeux de positions, minuteurs enter/exit, séquencement du tracé à froid, repeinte du voile sur horloge murale), qui ne remonte jamais. Explique FR-71: la respiration est une déformation locale sur des positions fixes, donc 'translation 0px, les cibles ne voyagent jamais' est STRUCTUREL. AD-8: la signature devient (modèle, positionsPrecedentes, graine, mode); mode porte mode de zone ET fond de nœuds; la densité réserve AU MAXIMUM donc elle n'est jamais un relayout (sinon 4e action interdite par FR-16, ou chevauchement interdit par FR-13). AD-9: la COMPOSITION est résolue dans la scène, pas laissée à l'alpha du rasteriseur — sans quoi le gate '3:1 sur le pire champ composité' d'AD-28 n'est pas calculable du tout. AD-3: ajout d'un compteur d'invocations du layout, testé à zéro sur filtre/sélection/recherche/panneau — le déterminisme ne peut pas détecter un APPEL INJUSTIFIE, il rend la même réponse. AD-12: le seuil du voile est un âge ABSOLU strictement supérieur au plus grand intervalle sélectionnable plus marge, ce qui est la vraie lecture de FR-5. AD-23: le fichier de tokens porte les ONZE espaces de noms de DESIGN.md, pas la couleur seule — stroke porte la longueur de stub qu'AD-29 cliquette, opacity porte les exemptions d'AD-28 et le clamp de luminance différé, shape porte la géométrie de coque sans laquelle AD-8 est inimplémentable, layout porte la largeur de canvas qu'AD-29 mesurait en dur. D'où l'arête layout->tokens. AD-26: toute assertion géométrique NOMME sa phase de mouvement (la respiration fait bouger un chiffre cliquetté à elle seule); et limite nommée — une assertion de scène ne teste jamais la rastérisation d'un glyphe, donc NFR-20 sort du périmètre automatisable. AD-28: l'ensemble des exemptions est DONNEE dans le fichier de tokens avec son propre plancher, pas codé en dur — le voile n'est pas exempt mais planché à 4.2:1. AD-16: 'NFR-3 est rempli par' adouci — un garde in-process n'est pas une frontière de capacité. AD-17: coût nommé — le défaut 127.0.0.1 met Portolan hors de portée de la persona exécutif et du critère de réussite #3. AD-20: densité et mouvement (FR-61) ajoutés; FR-45 est un signal d'OS, pas une préférence — l'OS peut taire le mouvement, la préférence stockée ne peut jamais le rallumer.
- (constraint) CONTRADICTION AMONT TRANCHEE ICI, A REMONTER: DESIGN.md laisse density piloter spacing.cell-clearance, ce qui fait du contrôle de densité (FR-42) une QUATRIEME action de relayout, interdite par FR-16. Arbitrage retenu: FR-16 ('les positions sont gagnées et gardées') est la thèse de stabilité du produit et pèse plus qu'un contrôle d'affichage. Résolution: le layout réserve les coques à la densité MAXIMALE, la densité ne fait plus que rétrécir les corps rendus dans de l'espace déjà réservé. cell-clearance ne peut plus suivre la densité. Coût: on tient moins d'objets qu'un circle packing — coût que §6 porte déjà.
- (event) REVIEWER GATE: lint déterministe + 4 lentilles parallèles (rubric walker; actualité technique web-vérifiée; attaque adversariale conforme-mais-incompatible; cohérence interne). Verdict consolidé REVISE. 8 AD nouveaux (AD-38..AD-45), AD-18 RETIRE, ~20 AD amendés. Rapports dans reviews/.
- (version) CORRECTION D'UNE AFFIRMATION ANTERIEURE DE CE MEMLOG (entrée du 2026-09-14 sur V8/fdlibm). Le déterminisme flottant n'est PAS quasi gratuit en JS de façon générale. Seuls + - * / et Math.sqrt sont exactement spécifiés par IEEE754 et arrondis identiquement par tout moteur conforme. ECMAScript déclare les transcendantes implementation-approximated DELIBEREMENT, pour laisser les moteurs utiliser la libm de la plateforme; la fdlibm embarquée de V8 était un accident historique que V8 défait actuellement (Math.tanh repassé sur std::tanh, V8 14.8.57 / Chrome 148). Trou plus grave encore: le layout tourne dans le NAVIGATEUR (Chromium, Firefox, Safari par NFR-16) alors qu'AD-32 ne teste le déterminisme que sous Node/V8 — deux moteurs jamais couverts par le test. Correction retenue dans AD-8: les transcendantes rejoignent la liste des interdits du layout, à côté de Math.random et des lectures d'horloge. Le déterminisme devient inter-MOTEURS par construction, ce qui est plus fort que l'objectif initial.
- (decision) AD-38 CHAQUE VALEUR DERIVEE A EXACTEMENT UN PROPRIETAIRE CALCULANT. C'est le diagnostic de fond de l'attaque adversariale (19 paires conformes-mais-incompatibles): la spine gouvernait rigoureusement les ETAGES et les DETENTEURS D'ETAT, et ne disait rien des VALEURS DERIVEES. Une fonction nommée, dans un package nommé, exportée, importée partout ailleurs. Table: coque de silhouette (model) -> layout réserve, scene dessine; portée transitive à N sauts (model) -> highlight FR-22, Keep only this FR-32, ego-graph FR-20; teinte et octave de réseau (model, depuis l'ordre de création) -> champs, badges, légende; ensemble et compteur d'orphelins (model); masquage d'une valeur (scene); niveau de lecture depuis le zoom (scene); contenu de légende et extents (scene). Prevents: deux coques qui diffèrent d'une règle d'arrondi font chevaucher les bulles; deux parcours de portée font diverger ce que Keep only this garde; deux attributions de teinte repeignent le chart entre deux sondages.
- (decision) AD-39 LA CAMERA EST DE L'ETAT DE VUE, ET LA SCENE EST FONCTION DU NIVEAU DE LECTURE, PAS DE LA CAMERA. Pan/zoom/cadrage vivent dans view-state. La scène est construite pour un NIVEAU DE LECTURE quantifié (FR-15), jamais pour une valeur continue de caméra; dans un niveau, la caméra est une transformation que le rasteriseur applique à une scène qu'il ne reconstruit pas; franchir une frontière de niveau la reconstruit. Prevents: la caméra sans propriétaire du tout — ni dérivée du sondage (AD-2) ni dérivée de l'horloge — où un epic en fait une entrée de scène (ce qu'exigent FR-15 et FR-47) et l'autre une transformation de rasteriseur (ce qu'exige NFR-8), chacun détruisant ce dont l'autre a besoin. C'est exactement à quoi sert FR-66 et pourquoi FR-14 peut promettre 'le zoom change la netteté, jamais la population'. AD-40: le layout place dans un espace ABSTRAIT, non borné, sans pixels — sinon redimensionner la fenêtre devient un 4e relayout que FR-16 interdit et que le compteur d'AD-3 ne voit pas, l'appel passant par un paramètre légitime.
- (decision) AD-41 LES FILTRES POSENT UN ETAT DE PRESENTATION, ILS NE RETIRENT JAMAIS D'OBJET DE LA SCENE. Tout objet du modèle est dans la scène, porteur de: plein, atténué-par-highlight (FR-33), retiré-par-filtre, ou pâle-de-filtre-vide (FR-34). Prevents: FR-83 et FR-34 INEXPRIMABLES — si un filtre retire les objets avant construction de la scène, la recherche ne peut plus rapporter un résultat comme 'trouvé et filtré' (elle mentirait sur ce que contient le cluster, la seule chose que Portolan existe pour ne pas faire) et les exclus ne peuvent pas revenir en contexte pâle. AD-42: le modèle a une FORME DE FIL déclarée et les deux conversions, possédées par le package partagé, avec test d'aller-retour — sinon Map, Set, cycles et instances de classe rendent AD-4 vrai seulement dans le vérificateur de types et faux sur la seule frontière qui compte.
- (decision) AD-43/AD-44/AD-45 — DIMENSIONS ENTIEREMENT MUETTES, trouvées par le rubric walker. AD-43 ENVELOPPE DE BUILD: npm workspaces (livré avec Node, aucun outil de plus), Vite pour le bundle navigateur et le serveur de dev, serveur compilé en avance jamais transpilé au runtime, image multi-étage (build puis copie de la sortie et des dépendances de prod dans node:24-alpine). Rend AD-23 IMPLEMENTABLE: tokens écrits une fois en TypeScript typé, CSS custom properties GENEREES par une étape de build et vérifiées en CI, jamais éditées à la main — sans format ni génération, 'un fichier lu par le CSS, le TS et un runner Node' est un souhait. AD-44 OBSERVABILITE ET ECHEC CLIENT: logs structurés sur stdout uniquement, niveau par variable d'environnement, aucun fichier (AD-19); le navigateur a EXACTEMENT TROIS écrans pleine surface déjà spécifiés (FR-57, FR-60, AD-45) et AUCUN QUATRIEME — tout autre échec client laisse la carte debout et la pâlit selon FR-54, parce qu'un produit dont la promesse centrale est 'la carte reste' ne la remplace pas par une trace de pile; CSP même origine, SSE même origine; préférences versionnées et JETEES plutôt que migrées en cas de désaccord. AD-45 remplace AD-18 retiré: au démarrage Portolan JOURNALISE, dans le registre de FR-57, l'adresse exacte sur laquelle il écoute, sur quel nœud, et ce qu'il faut changer — docker service logs est la seule surface garantie de fonctionner quand la surface HTTP ne l'est pas.
- (constraint) ERREUR PROPRE CORRIGEE: AD-18, que l'assistant avait INVENTE au checkpoint et fait ratifier sans le tester, est LOGIQUEMENT IMPOSSIBLE — un serveur que le navigateur ne peut pas joindre ne peut pas servir l'écran qui dit qu'il est injoignable. AD-18 est retiré, son identifiant réservé et jamais réutilisé, et AD-45 répond au besoin réel. Autres corrections notables du gate: les clés d'identité d'AD-5 n'étaient PAS qualifiées par le genre (volume:web et network:web étaient le même objet, avec la même silhouette et la même cellule); AD-33 mettait mode dans la clé de surface ET dans les paramètres de layout, ce qui produisait exactement le téléport que FR-40 interdit; le masquage fuyait par le panneau de détail (carte propre, premier clic = plan d'adressage au mur); FR-46 (export PNG) n'avait aucun propriétaire; l'API Docker n'était épinglée nulle part et AD-27 masquait la dérive; TypeScript était épinglé deux majeures en retard (7.0 stable depuis le 2026-07-08, compilateur natif Go).
- (event) spine finalized — 45 AD (AD-18 retiré), 4 diagrammes mermaid valides, lint 0 finding, statut final. Reste ouvert et assumé: 12 entrées en Deferred, chacune avec sa condition de revisite et son propriétaire nommé; 7 écarts à remonter au PRD et aux spines UX.
