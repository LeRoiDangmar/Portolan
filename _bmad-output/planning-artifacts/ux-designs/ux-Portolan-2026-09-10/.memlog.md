---
topic: Portolan UX — DESIGN.md + EXPERIENCE.md
updated: 2026-09-11T09:06
---

- (decision) Sources confirmed by Jules: briefs/brief-Portolan-2026-09-09/brief.md and addendum.md both apply.
- (decision) Product: Portolan is a graphical visualization interface for a containerization environment (Docker Swarm / Kubernetes-class orchestrators).
- (decision) Visual posture (user's words): 'slick'. Centerpiece is a beautiful interactive graph — pan, click, zoom, filter, display zones/regions.
- (assumption) No reference products, no anti-patterns and no hard constraints (platform, design system, existing brand, a11y, regulatory) named at brain-dump time. To be probed.
- (event) Brain dump captured; source extraction dispatched.
- (decision) [from brief] Core UX thesis: 'A topology tool, not another dashboard.' 'The edges are the actual product.' Bar: 'legible over impressive.'
- (decision) [from brief] 2D graph, 3D retired 2026-09-09. Colour is the primary encoding channel: 'Colour carries network membership.'
- (decision) [from brief] Zoom vs filter is a hard distinction: 'Filters are structural, not cosmetic. Zoom moves you closer; only filtering takes things away.'
- (decision) [from brief] Progressive disclosure: 'a picture you can walk into to get the text' - clicking anything opens IPs, image tags, mounts, placement.
- (decision) [from brief] Dual-audience legibility bar: a non-specialist can follow the picture; the same picture gives an expert the cluster at a glance.
- (decision) [from brief] Read-only. One swarm. Runs in-cluster, viewed in a browser. Polled refresh 5-10s configurable. Object types v1: nodes, networks, volumes, stacks, services, containers, images + edges.
- (decision) [from brief] User-adjustable presentation is a stated requirement: what is shown, colours, text size.
- (decision) [from brief] Two filter-shaped jobs, verbatim: 'show me everything' and 'show me only what touches backend'.
- (decision) [from brief] Screenshot is a first-class output path for v1; top external success signal is a stranger posting a screenshot of their own cluster. Default-view screenshot-worthiness is a real UX requirement.
- (decision) [from brief] Personas: 'the inheritor' (handed an undocumented cluster) is primary; 'the Swarm operator' (homelabber/solo dev/agency) is the tribe; the author is first user. PaaS-unaware users excluded 2026-09-09.
- (decision) [from brief] Canonical unanswered question the product exists to answer: 'which container sits on which network and uses which volume'. Current workaround: 'You open six tabs and hold the graph in your head.'
- (decision) [from brief] Tone: shows rather than judges. 'Portolan does not tell you something is wrong; it shows your infrastructure clearly enough that you see it yourself.'
- (assumption) [from brief, OPEN] Default legibility before any filter is applied is called the product's main open engineering question - the hairball risk. Weave Scope, the only shipped full relational graph, died with an open edge-legibility issue (#1636).
- (assumption) [from brief, OPEN] 'Are images really graph nodes?' Explicit open rendering decision: first-class node vs detail-panel attribute vs optional group-by-image view. 30 containers may share 3 images = heavy convergent fan-in.
- (assumption) [GAP] Brief does not address: accessibility (notable - colour is the primary encoding and colour-vision deficiency is never mentioned), dark mode, i18n, mobile/tablet/responsive, browser matrix, front-end stack or graph library.
- (assumption) [GAP] The name 'Portolan' is never explained in the brief - no nautical/portolan-chart/compass imagery. The docs lean on a plain map/cartography metaphor ('the way you reach for a map in an unfamiliar city'). Nautical framing is an open session decision, not a documented one.
- (event) Sources extracted by subagent: brief.md + addendum.md.
- (decision) [from brief] Stakes: open-source AGPLv3, publicly ambitious, single-author. Not commercial, not regulated. 'There is no technical moat... The moat is execution' - i.e. design quality is the differentiator.
- (decision) Scope confirmed by Jules: v1 IA models Docker Swarm only, with Swarm-native vocabulary on screen (node, network, volume, stack, service, container, image). Kubernetes stays a possible future collector and is out of both spines. Upholds the brief's written decision.
- (decision) Working mode: coaching path - decisions walked one at a time, creative tools (colour themes, design directions, Excalidraw wireframes, key-screen mocks) woven in.
- (decision) ZONES defined by Jules: a zone is an enclosing area drawn around the things that share a network - 'entourer les stacks dans le meme reseau avec une zone pour ce reseau'. Zones render networks as areas/hulls, not as edges. Shape and movement must feel 'vachement organique'.
- (decision) IA - three views, switched by TABS AT THE BOTTOM of the screen, each a complete change of view: (1) NODE VIEW = landing/overview of the swarm, nodes with e.g. container count per node; (2) STACK VIEW = all stacks with their networks, network interconnections, volumes; (3) SERVICE VIEW = precise per-service view with all available info.
- (decision) Chrome: LEFT MENU carries filters and display toggles (show/hide volumes, networks, etc.). BOTTOM TABS switch views entirely. Graph canvas is everything else.
- (decision) Visual language, Jules's own words: bubble diagram - 'format de bulles avec des lignes qui relient les choses entre elles et des aires pour representer les reseaux'. Bubbles float and are connected by lines.
- (decision) DISTINCTIVE PRIMITIVE - bubble protrusions: 'Les bulles auraient de petites excroissances la ou elles sont reliees avec le nom de la chose reliee'. Each connection point on a bubble is a small labelled nub naming what it connects to. This is the signature element and is nowhere in the brief - it is Jules's invention.
- (decision) Depth is stylistic, not spatial: 'de jolies effets d'ombre et de 3D pour styliser, le diagramme en lui-meme n'est pas en 3D'. Reconciles with the brief's 2D-not-3D decision - elevation and shading are allowed, spatial 3D is not.
- (decision) Questions the product must answer, Jules's phrasing: 'est-ce que le service X fait partie du reseau Y' (membership) and 'est-ce que la stack A est liee d'une maniere ou d'une autre au volume B' (TRANSITIVE reachability - implies a path/trace primitive, not just adjacency).
- (assumption) [OPEN] Zone overlap geometry unresolved. A container on two networks belongs to two zones. Hull-based grouping breaks or needs a rule when memberships intersect. Not answered by Jules yet.
- (assumption) [OPEN] Filter-vs-zoom status of zones not answered. Jules's description implies zones GROUP rather than REMOVE, with separate filter toggles doing the removing - but unconfirmed against the brief's rule 'only filtering takes things away'.
- (assumption) [OPEN, TENSION] Double encoding of network membership: the brief says 'colour carries network membership'; Jules's zones also carry network membership. Either reinforcement, or colour is freed for another dimension. Undecided.
- (assumption) [OPEN, TENSION] 'Organique en terme de deplacement' + floating bubbles implies continuous motion. Continuous motion on a relational graph fights the brief's 'legible over impressive' bar (things move while you read them) and raises a vestibular-accessibility concern. Not yet probed.
- (assumption) [OPEN] Form-factor not yet confirmed. Left menu + bottom tabs + large canvas implies desktop/laptop browser; bottom tabs is also a mobile idiom. Needs explicit resolution before IA closes.
- (event) Discovery: zones, three-view IA, chrome layout and bubble visual language captured from Jules.
- (override) OVERRIDES the brief's 'colour carries network membership'. Jules: 'chaque bulle aura des pastilles de couleurs et de formes differentes pour montrer l'appartenance a une stack, un reseau, sa sante, un type etc... Mais on garde les zones pour les reseaux.' Encoding moves from bubble-fill (one dimension) to a SET OF BADGES on each bubble - each badge a family, coded by colour AND shape. Networks remain carried by zones.
- (decision) PASTILLE SYSTEM is the core encoding primitive. Badge families named so far: stack membership, network, health, object type - list is open. Colour+shape double-coding means the encoding survives colour-vision deficiency without a separate a11y mode. This answers an accessibility gap the brief never identified.
- (decision) MOTION: 'vivant en permanence' - bubbles float and breathe continuously, including at rest. Jules chose this with the legibility cost stated explicitly (drifting targets are harder to read; pushes against the brief's 'legible over impressive'). Decision stands; mitigations to be designed, not the decision revisited.
- (decision) FORM-FACTOR CLOSED: desktop/laptop browser only, 1440px and up. Below that, an honest message rather than a degraded rendering. No tablet target, no mobile target.
- (decision) Interaction primitive spotted and named, not yet specified: TRACE A PATH between two objects. Comes from Jules's own question 'est-ce que la stack A est liee d'une maniere ou d'une autre au volume B' - 'd'une maniere ou d'une autre' is transitive reachability, which neither zones nor a glance can answer on a large cluster.
- (assumption) [OPEN, TENSION] Pastille density at scale. Several badge families on every bubble across a few hundred containers is a lot of marks. Directly compounds the brief's main open question - default legibility before any filter. Likely mitigation: the left menu governs which badge families are shown. Unconfirmed.
- (assumption) [OPEN] Continuous motion requires a prefers-reduced-motion path as an accessibility floor item. Not a revisit of the motion decision - a standard requirement alongside it.
- (decision) CONCERN SCAN - carried by Portolan: dark mode; internationalisation; offline/air-gapped operation (no CDN, no Google Fonts, everything embedded in the image); input modalities for pan/zoom (trackpad vs wheel vs drag vs keyboard); adjustable density and text size (promoted to a first-class concern, per the brief); perceived performance on a large cluster (loading states, the 5-10s refresh, distinguishing 'it is loading' from 'it is alive' on a permanently animated graph).
- (decision) CONCERN SCAN - explicitly NOT carried: keyboard traversal of the graph and screen-reader support. Raised by the facilitator, not selected by Jules. Colour-vision deficiency remains covered by the colour+shape pastille double-coding. EXPERIENCE.md's Accessibility Floor is therefore scoped to: colour+shape double-coding, adjustable text size, reduced-motion path - and states the keyboard/SR exclusion plainly rather than implying coverage.
- (override) OVERRIDES the brief's deferral of export. Brief said 'SVG/PNG export - wanted later, screenshot accepted for now'. Jules promotes EXPORT SVG/PNG TO V1. Consequence flagged at decision time: export changes the design of the default view, since the exported frame is what strangers post - the brief's top external success signal.
- (event) Creative tool: Excalidraw IA + chrome wireframe rendered to .working/ia-portolan-2026-09-10.excalidraw (61 elements). Covers the three-view IA, left filter menu, bottom tabs, conditional detail panel, and an explicit open-questions box.
- (assumption) [NOTE] Excalidraw renderer caps at 61 elements - only 62 valid two-character indices exist (a0-az). Any future wireframe needing more detail must be split across several files.
- (decision) JOURNEY - first 5 seconds, Jules's words: 'il voit cash la vue entiere dezoomee de toutes ses stacks avec toutes les infos, les pastilles etc.' Landing state is the WHOLE cluster, fully zoomed out, nothing hidden, pastilles on.
- (decision) JOURNEY - the climax beats, three discoveries, all cleanup-shaped: (1) an ORPHAN container; (2) a container on a network it has no business being on; (3) two volumes with almost the same name attached to the same container, one of which should be deleted. All three are anomalies found BY EYE - consistent with the brief's 'Portolan does not tell you something is wrong; it shows your infrastructure clearly enough that you see it yourself.'
- (decision) DESIGN CONSEQUENCE of discovery 2: the anomaly is a PASTILLE/ZONE MISMATCH - a bubble sitting inside the 'frontend' zone whose stack pastille says something else. The pastille system is what makes this visible; it is not decoration. No rule engine needed - the encoding does the work.
- (decision) DESIGN CONSEQUENCE of discovery 3: near-identical volume names on one container ('pgdata' vs 'pg-data') can only be caught by reading two labels ADJACENTLY. This is exactly what Jules's labelled protrusions ('excroissances avec le nom de la chose reliee') deliver. The signature primitive earns its place - it is the mechanism of a climax beat, not styling.
- (assumption) [OPEN, TYPOGRAPHY] Discovery 3 imposes a DESIGN.md requirement not yet decided: identifiers must be rendered in a face where 'pgdata' / 'pg-data' / 'pg_data' / 'pgdatal' are unmistakable at small sizes. Points to a monospace or a highly-differentiated face for object names. Jules has not chosen.
- (assumption) [CONFLICT] Landing view contradicts an earlier statement. Earlier: 'l'admin arrive sur l'app avec une vue d'ensemble du swarm, avec les noeuds et le nombre de conteneurs dans chaque noeud' = NODE VIEW lands. Now: the first 5 seconds are 'la vue entiere dezoomee de toutes ses stacks' = STACK VIEW lands. Both cannot be the default. Unresolved.
- (assumption) [OPEN, IA GAP] An orphan container has no stack. If the stack view is the landing surface, orphans have no home in it - yet finding orphans is climax beat 1. The stack view needs an explicit place for the stack-less, or orphans are invisible exactly where they must be found.
- (assumption) [OPEN, TENSION] 'Toutes les infos' at full zoom-out collides with physics: text has a minimum readable size, so a few hundred containers cannot all carry legible labels at once. This IS the brief's main open question (default legibility before any filter), now sharpened. Level-of-detail is the obvious answer but sits against the brief's rule 'zoom moves you closer; only filtering takes things away'.
- (decision) LANDING SURFACE RESOLVED: a single overview where stacks and their network zones are the foreground, and the swarm NODES are drawn beneath as a background layer (bands/regions) saying where each thing runs. Answers 'what' and 'where' in one glance. This MERGES two of the three originally-stated tabs - the node view is no longer a separate destination.
- (decision) ORPHANS: a container with no stack floats freely between the zones, attached to nothing. Its visual isolation IS the signal - everything else is grouped, it is not. No dedicated 'no stack' region, no dedicated badge. Accepted cost: on a large cluster an orphan can recede into the background; mitigation to be designed.
- (override) OVERRIDES/REFINES the brief's rule 'Zoom moves you closer; only filtering takes things away'. Jules chose PROGRESSIVE LEVEL OF DETAIL, with the friction against that rule stated at decision time. Reconciled formulation: ZOOM CHANGES SHARPNESS, NEVER POPULATION. FILTER CHANGES POPULATION, NEVER SHARPNESS. Nothing is ever removed by zooming - the set of objects present is identical at every zoom level; only how much text each object renders changes.
- (decision) LOD LADDER: zoomed out = network zones, bubbles, pastilles, and only the large labels (stack names, network names). Zooming in reveals service names, then container names, then the protrusion labels. The protrusion labels - the mechanism of climax beat 3 - sit at the deepest LOD rung, reached by zooming, not by clicking.
- (assumption) [OPEN, IA CLOSURE FAILURE] The service view now has no journey landing on it. All three of Remi's discoveries happen in the overview. Per surface closure, every surface needs a journey that lands there - the service view currently does not have one. Either a journey is missing, or the service view is a detail surface rather than a view.
- (assumption) [OPEN] Tab structure unresolved after the merge. Originally three bottom tabs (nodes/stacks/services). Nodes is now a background layer of the overview. Open: how many tabs remain, and whether the service view is a tab or a drill-down.
- (assumption) [OPEN] Detail panel vs service view overlap. The brief specifies a detail panel opening on click (IPs, image tags, mounts, placement). Jules specified a service view showing 'toutes les infos possibles' for a service. These may be the same surface described twice.
- (decision) IA CLOSED - three bottom tabs: (1) VUE D'ENSEMBLE (landing) stacks + network zones + volumes, nodes as a background layer; (2) VUE NOEUDS, dedicated, nodes in the foreground - answers a different question: 'what runs on node-03 and is it loaded?'; (3) VUE SERVICE, an ego-graph centred on one service. Plus a side DETAIL PANEL, not a tab.
- (decision) DETAIL PANEL and SERVICE VIEW are two distinct surfaces. Panel = factual text (IPs, image tags, mounts, placement), opens on clicking anything. Service view = a bubble graph of one service's neighbourhood, isolated from the rest of the cluster. Text on one side, a map on the other.
- (decision) REACHABILITY PRIMITIVE: clicking an object lights up everything reachable from it and dims the rest. Answers 'est-ce que la stack A est liee d'une maniere ou d'une autre au volume B'. Dilution risk was stated at decision time - on a well-connected cluster almost everything lights up.
- (assumption) [OPEN, COLLISION] Two behaviours are both bound to a single click: opening the detail panel, and lighting the reachable set. Probable resolution - one click does both (click = select: panel opens AND reachable set lights). Needs confirmation, and needs a defined way to deselect.
- (assumption) [OPEN] Reachability dilution mitigation undecided. Candidates: hop-distance falloff (1 hop bright, 2 dimmer, 3 faint), a hop-limit control, or edge-type restriction. Not yet probed.
- (assumption) [OPEN] Vue service still has no journey landing on it even though it is now a tab. Surface closure not yet satisfied for that surface.
- (decision) MODE: dark by default, light available. Both palettes must be carried, and overlapping translucent zones must work in both. Noted at decision time: screenshots pasted into light-background docs/tickets are a real context, since the brief's top external success signal is strangers posting screenshots.
- (decision) BRAND REGISTER: 'cartographie assumee' - the visual vocabulary of real charts. Rhumb lines, paper-and-ink tones, coastline hatching, cartographer's typography, a legend treated as a chart legend. Jules chose this over discreet allusion, purely technical register, and modern-topographic. The nautical origin of the name Portolan becomes explicit design material - a decision the brief never took.
- (event) Creative tool: zone-overlap decision aid rendered to .working/zone-overlap-options-2026-09-10.html - four candidate geometries for overlapping network zones (intersecting hulls / disjoint hulls + echo / exclusives-in-hull + shared corridor / tinted contour field), same 7-entity cluster in all four, 86 labelled protrusions total, none ranked.
- (assumption) [NOTE] Both creative artifacts were verified analytically, not visually - no browser or SVG rasteriser in the renderer environment. Jules's own eye on the files is the real check.
- (decision) ZONE RENDERING - TWO SWITCHABLE MODES, toggled from the left display menu, not one geometry. Mode A 'champs de teinte' (soft blended tint fields + dashed contour isolines, no closed outlines) is THE DEFAULT and therefore the screenshot frame. Mode B 'blobs disjoints + echo' (hard disjoint boundaries, multi-network objects echoed as ghost bubbles) is the precise alternative. Switching re-lays-out the map - it is a movement, not a style swap, and must be designed as an animated transition.
- (decision) ECHO EDGE OWNERSHIP (mode B): each copy carries the edges of ITS OWN zone. The copy in 'frontend' carries frontend links; the echo in 'backend' carries backend links and the pgdata mount. Each zone reads correctly on its own with no edge crossing the map. Accepted cost: neither copy shows the object in full.
- (decision) LAYOUT STABILITY - FIRST-ORDER REQUIREMENT, named in session, absent from the brief: positions are earned and kept. Across the 5-10s poll nothing moves. A new container slides in near its neighbours; a vanished one fades in place. A manual 'Reorganiser' button exists if the map deforms over time. Rationale: Remi must be able to say 'the odd thing was top-left' and find it again.
- (decision) SYNTHESIS - the two channels catch each other: the tint field gives the fuzzy cartographic read; the NETWORK PASTILLE on each bubble gives the exact binary answer to 'is service X on network Y'. The default rendering's known weakness (blurred network boundary) is covered by the pastille system rather than by adding precision to the zones.
- (assumption) [OPEN] Still unspecified for EXPERIENCE.md: state patterns (first load, empty cluster, socket unreachable, refresh failure / stale data), voice and tone, pan-zoom input modalities, i18n posture, SVG/PNG export behaviour, the deselection gesture, and the journey that lands on the service view.
- (decision) PAN/ZOOM: map convention (Google Maps / Figma). Wheel and two-finger = zoom; drag on background = pan; space+drag also pans. Chosen WITHOUT the keyboard-accelerator variant - so there are no keyboard shortcuts. Derived requirements: deselection is a click on empty background (no Escape), and 'fit to whole cluster' must exist as an on-screen control since there is no '0' key.
- (decision) STALE DATA: on refresh failure the map STAYS, timestamped and visibly ageing - explicit 'last read N min ago', with the map slowly paling and desaturating. You keep working on a map you know is ageing. Fits the cartographic metaphor: an out-of-date chart is still useful. No error screen, no hiding the map.
- (decision) I18N: English by default, strings externalised from the start, French shipped. A community contribution can add a language without touching code. Interface language is English even though Jules works in French.
- (decision) SERVICE VIEW - closure satisfied. The journey that lands there: Remi has spotted a suspect service in the overview and isolates it to see it alone - its networks, volumes, containers and immediate neighbours - without the other 200 objects. It is a SEMANTIC zoom, not a geometric one. This is what distinguishes it from zooming in on the overview.
- (assumption) [CONFIRM AT FINALIZE] The protagonist name 'Remi' was supplied by the facilitator as an example, not by Jules. Jules built on it but never named the character himself. Confirm or rename before the journey goes into EXPERIENCE.md.
- (decision) EXPORT: what you see is what you get - current framing, zoom and active filters are respected literally. Cost stated at decision time and accepted: under progressive LOD, a zoomed-out export ships without the fine labels.
- (decision) VOICE AND TONE: chart vocabulary for the CHASSIS (product actions and states - surveyed, off-chart, reorganise, fit to chart), Docker vocabulary strictly intact for anything naming a REAL cluster thing - service, network, volume, stack, node are never renamed. An object's name stays what an admin would type in their terminal. UI language is English.
- (decision) FIRST LOAD: the map draws itself in front of you - node backdrop first, then network zones, then bubbles, then edges. Loading IS the product's first movement; the user learns the structure by watching it appear in layers. Risk to design against: on a permanently-alive graph it must be unmistakable when loading has finished.
- (event) EXPERIENCE.md scope complete: IA, states, interactions, voice, journey, a11y floor and platform all decided.
- (decision) PASTILLE FAMILIES - four, all retained: object type (service/container/volume/image/stack), stack membership, network (a multi-network object carries several badges of this family), and health. Each family coded by colour AND shape.
- (decision) PASTILLE DENSITY: all families are always present, governed by the LOD ladder rather than by a toggle. Zoomed out, only the most structural family renders; the others appear as you approach. Consequence: the left menu does NOT toggle pastille families - it keeps toggling object types (volumes, networks, images).
- (assumption) [OPEN] Health pastille was retained despite the facilitator flagging it against the brief's 'Portolan does not tell you something is wrong'. Defensible framing to confirm: Docker's raw state is a FACT ('3/5 replicas', 'stopped'), whereas a red warning badge is a VERDICT. The distinction decides whether the health family breaks the brief's stance or respects it.
- (decision) LOD RUNG 1 - the family that survives maximum zoom-out, and therefore the one on the landing screenshot: OBJECT TYPE. From far away you see WHAT things are before you understand them - serving the brief's non-specialist bar. Stack, network and health families appear as you approach.
- (override) DEPARTS FROM THE BRIEF, deliberately and with the cost stated at decision time. Health pastille is a GREEN/AMBER/RED traffic light, not a neutral fact. The brief says 'Portolan does not tell you something is wrong; it shows your infrastructure clearly enough that you see it yourself.' A traffic light is the tool rendering a verdict. Jules chose it over both the neutral-fact and the stopped-only-colour options. EXPERIENCE.md and DESIGN.md must state this as a knowing departure rather than quietly contradicting the brief.
- (decision) PASTILLE ENCODING: SHAPE = FAMILY, COLOUR = VALUE. All health badges share one shape, all network badges another, etc. The shape tells you which dimension you are reading; colour alone tells you which value.
- (override) CORRECTION to an earlier session claim. The facilitator earlier stated the pastille system solved colour-vision deficiency 'accidentally'. With shape=family / colour=value that is FALSE at the value level: a colour-blind user knows they are looking at a health badge but cannot read the state. EXPERIENCE.md's Accessibility Floor must state plainly that value-level distinctions are not covered, rather than implying coverage.
- (assumption) [OPEN, FOR PALETTE] Lever still available without reopening any decision: the health light need not be green/amber/red. A blue/amber/red light reads identically for a trichromat and survives deuteranopia. Same gesture, same three states, same immediacy - different starting hue. To settle when the palette is chosen.
- (event) Creative tool: four design directions rendered to .working/ - direction-1-portulan-ancien, direction-2-instrument-de-precision, direction-3-carte-marine-officielle, direction-4-cartographie-editoriale. All within the 'cartographie assumee' register, dark hero + light strip, same cluster and same zone geometry so only personality differs. Each documents its hex values in style comments.
- (change) REJECTED across all four directions: 'les bulles sont trop rondes et le tout pas assez organique'. This is shape-language feedback, not a direction choice - none of the four delivered the organic quality Jules asked for at brain-dump time ('vachement organique en terme de forme et de deplacement'). Circles are not the shape vocabulary. Needs a dedicated shape study before any direction can be picked.
- (decision) ORGANIC DEFINED by Jules, two things combined: (1) IRREGULAR CONTOURS - no bubble is a circle, each has its own slightly deformed outline like a pebble or a cell, no two alike, stable enough that you recognise an object by its silhouette as much as by its name; (2) SOFT BODIES THAT DEFORM - bubbles squash against neighbours and stretch toward what they are connected to, so the silhouette carries the relationships. Rejected: liquid fusion/metaballs (would dissolve the edges, and 'the edges are the product') and hand-drawn wobble.
- (decision) DIRECTION: Jules picks 2 'Instrument de precision' as the base - deep near-black ground, hairline rules, graduated reticle and register marks, rare metallic accents, minimal texture. He also states he likes 1 'Portulan ancien' (night parchment, sepia, oxidised brass, rhumb-line web, coastline hatching). A 1+2 blend is to be tested, not assumed.
- (assumption) [OPEN] Does the irregular silhouette carry MEANING or is it decorative randomness? If deformation encodes connections, shape becomes a data channel alongside pastilles and zones. If silhouettes are stable-but-arbitrary, they serve recognition only. Undecided - the shape study must expose both readings.
- (decision) DIRECTION CONFIRMED: 'prends 2' - Instrument de precision, PURE. No blend with direction 1. Deep near-black ground, hairline rules, graduated reticle and register marks, rare metallic accents, minimal texture.
- (decision) ORGANIC, restated verbatim by Jules and confirmed as the binding definition: 'contours irreguliers (galets, cellules) + corps mous qui se deforment. Les deux ensemble : chaque bulle a sa silhouette propre, et elle se deforme sous l'effet de ses relations et de ses voisines.' BOTH criteria together - this is shape treatment D in the running study, not B or C alone.
- (decision) TYPOGRAPHY: monospace for identifiers (anything naming a real cluster object - pgdata, pg-data, shop_api.1), sans-serif for chassis labels. The typographic split doubles the voice rule already decided (chart vocabulary for chassis, Docker vocabulary for objects), and a fixed pitch solves pgdata/pg-data by construction.
- (decision) FONTS: font files EMBEDDED IN THE IMAGE and served by Portolan itself. Identical rendering on every machine and in every screenshot - which matters because the brief's success signal is strangers posting screenshots. Costs a few hundred KB in the image and requires AGPL-compatible licences. Satisfies the air-gapped constraint.
- (decision) HEALTH LIGHT: BLUE / AMBER / RED, not green/amber/red. RESOLVES the accessibility hole opened by shape=family/colour=value: health was the one family whose value became unreadable under deuteranopia and protanopia. Same immediacy, same three states, standard data-visualisation substitution. EXPERIENCE.md's Accessibility Floor can now state that value-level colour distinctions are colour-vision-safe, while keyboard traversal and screen-reader support remain out of scope.
- (decision) SECOND JOURNEY: the Swarm operator / homelabber - the brief's 'tribe', who adopts fast and shares. His journey typically ends in a screenshot posted publicly, which is the brief's top external success signal, so he directly tests the quality of the default screen. To be drafted from brief material plus this session and flagged for Jules's correction - he has not narrated it himself.
- (assumption) [OPEN, CRITICAL AMBIGUITY] Jules chose 'excluded objects stay, extremely dimmed' for the empty-result state. Unclear whether this is (a) the GENERAL filter behaviour - filters only dim, never remove, which kills the brief's rule 'filters are structural, not cosmetic; only filtering takes things away' - or (b) only the fallback when a filter would empty the map. The two readings produce materially different products. Must be resolved.
- (decision) FILTER BEHAVIOUR RESOLVED - the brief's rule holds. A filter genuinely REMOVES; 'filters are structural, not cosmetic' stands. The extreme-dimming behaviour is a NAMED EXCEPTION for one case only: when a filter would empty the map entirely, the excluded objects return as very pale context so the user can see what their filter is too tight against. Not the general behaviour.
- (decision) JOURNEY 2 - the homelabber, Jules's words: 'Il veut montrer sa super infra a ses copains, quand il voit tous les reseaux, les interconnexions, les volumes, toutes ces infos - presque trop pour que ce soit facilement lisible mais tellement classe pour flexer devant ses collegues, c'est la qu'il prend le screen.' The screenshot moment is triggered by DENSITY AS PRIDE, not by a discovery.
- (assumption) [CENTRAL TENSION, surfaced by journey 2] The default screen has two masters. Remi needs it legible enough to spot an anomaly in 5 seconds; the homelabber needs it dense enough to impress colleagues - 'presque trop pour que ce soit facilement lisible'. The brief's headline bar is 'legible over impressive', yet its top external success signal (a stranger posting a screenshot) depends on someone screenshotting BECAUSE it is impressive. The brief contradicts itself and journey 2 exposed it. Same screen, both masters - must be arbitrated.
- (decision) CENTRAL TENSION RESOLVED - governing principle for both spines: 'BEAUCOUP A REGARDER, PEU A LIRE' (much to look at, little to read). Impressive density at the default zoom comes from network zones, blended tints, irregular silhouettes, edges and type pastilles - NEVER from text density. Text stays rare until the user has descended the LOD ladder. The homelabber captures a rich image; Remi never has to decipher. The brief's 'legible over impressive' is upheld by reading it as a constraint on READING LOAD, not on graphic richness. DESIGN.md owns the richness; EXPERIENCE.md owns the LOD ladder that delivers it.
- (event) EXPERIENCE.md distilled. Sections: Foundation, IA, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, Responsive & Platform, Inspiration & Anti-patterns, Key Flows (Remi + homelabber), Open Questions. Four override entries marked inline as [DEPARTS FROM BRIEF]. 28 distinct {path.to.token} references used - DESIGN.md must define every one.
- (assumption) [BLOCKER, CONTRADICTION found at distillation] Motion vs layout stability. 'Bubbles float and breathe continuously' (46) against 'across the 5-10s poll nothing moves' (83). Read strictly these conflict. Candidate reconciliation: motion is LOCAL DEFORMATION, not translation - the silhouette breathes while the centre holds. Needs Jules's ruling plus an amplitude bound.
- (assumption) [BLOCKER] 'Are images really graph nodes?' - the brief's own explicit open rendering question - was decided BY IMPLICATION and never explicitly: image appears as an object-type pastille value (95) and as a left-menu filterable type (96), but no decision was ever taken on whether an image is a bubble.
- (assumption) [BLOCKER] Node view has no journey landing on it. Its purpose is stated but no journey was ever narrated. Surface closure fails for that tab.
- (assumption) [BLOCKER] Two states never decided: empty cluster, and socket unreachable on COLD LOAD (distinct from stale data, which presumes a map already drawn).
- (assumption) [NON-BLOCKER, logged for later] Undecided and flagged in EXPERIENCE.md Open Questions: end-of-first-load signal mechanism; what prefers-reduced-motion actually stills; echo-bubble selection semantics (object vs copy); node backdrop selectability; service view entry mechanism and its empty state; export control placement (assumed into the left menu); whether the left menu's 'networks' filter removes the zones as well as the network objects; browser support matrix, never named anywhere.
- (assumption) [ASSUMPTION] Homelabber protagonist named 'Tom' by the distillation subagent - not chosen by Jules. Confirm or rename.
- (decision) MOTION vs STABILITY RESOLVED: motion is LOCAL DEFORMATION ONLY, never translation. The bubble breathes and its silhouette undulates; its centre does not move by a pixel. Both earlier decisions hold in full - alive AND you always find what you saw where you saw it. Amplitude applies to the outline, not the position.
- (decision) IMAGES ARE NOT GRAPH NODES - closes the brief's own explicit open rendering question. An image is an attribute read in a container's detail panel (e.g. nginx:1.25-alpine), never a bubble on the map. Avoids the convergent fan-in the brief named as 'the fuel for the hairball failure mode'. Accepted cost: you cannot see at a glance who shares which image.
- (change) PROPAGATION of the images decision: the object-type pastille family no longer has an 'image' value, and the left menu no longer offers an 'images' filter over the map. Both were listed earlier (95, 96) on the assumption images were rendered objects.
- (decision) NODE VIEW JOURNEY - surface closure satisfied: checking DISTRIBUTION. He wants to see whether load is spread - one node carrying twice as many containers as the others, or a stack sitting entirely on one machine when it was meant to be spread. The imbalance is the discovery.
- (decision) COLD-LOAD FAILURE (socket unreachable, no map ever drawn): a screen in the chart register that explains and guides - what is missing (manager-node socket access), why Portolan needs it, and the exact configuration line to fix. The brief makes socket access a first-order security concern; this screen is where it gets explained.
- (decision) EMPTY CLUSTER (fresh swarm, nothing deployed): the node backdrop renders normally - those machines are real information - and the rest of the map is empty space with a note in the chart register. A sea chart with no ships on it is still a chart.
- (decision) LEFT MENU 'Networks' filter, unchecked, removes BOTH the zones AND the network attachment edges. Everything expressing network disappears: no tint fields, no isolines, no attachment links. Objects and their volumes remain. A radical filter, since network is the product's primary axis.
- (decision) PREFERS-REDUCED-MOTION stills all CONTINUOUS motion - the permanent breathing stops entirely. Action-triggered transitions (filtering, switching zone rendering, selecting) stay animated, because they explain what just changed. Decorative motion removed, explanatory motion kept. The layered first draw is also kept.
- (decision) END-OF-LOAD SIGNAL: the draw finishes with a distinct resolution gesture - the last layer lands and everything settles at once, before the breathing begins. Visual, immediate, nothing to read. Resolves the risk that on a permanently-alive graph 'loading' and 'living' look the same.
- (decision) ASSUMPTIONS PROPOSED TO JULES for correction, uncontested so far: echo selection selects the OBJECT (both copies light); the node backdrop is not selectable; service view is entered by selecting a service then an 'isolate' control; the export control lives in the left menu; browser support is recent Chromium, Firefox and Safari.
- (event) All EXPERIENCE.md blockers and non-blockers closed. Ready for update pass.
- (event) EXPERIENCE.md updated with all 13 rulings. Added Flow 3 (node view / checking the spread), two Component Patterns rows (Image, Isolate), one State Patterns row (End of first load). Open Questions cut from 17 to 6. Final token count: 29 distinct {path.to.token} references that DESIGN.md must define.
- (decision) Reconciliation recorded by the distiller: networks are rendered as AREAS for membership (no hub-and-spoke network node), while attachment links remain real edges in the model. The 'no rendering choice may dissolve an edge' rule binds RENDERING; a filter is structural and may remove edges. Stated in the document so it does not read as a contradiction later.
- (change) STALE ARTIFACT: .working/ia-portolan-2026-09-10.excalidraw still draws an 'Images' filter in the left menu and still lists 'are images graph nodes?' as open. Both are now decided against. EXPERIENCE.md carries a drift note; the wireframe needs regenerating before promotion to wireframes/.
- (assumption) [OPEN, 6 remaining] 1. Default legibility before any filter (carried from brief - genuinely an engineering question needing a real test cluster). 2. Click collision - panel + reachability on one click, still [ASSUMPTION]. 3. Reachability dilution mitigation. 4. Does the irregular silhouette carry meaning - awaits the shape study. 5. Orphan salience on a large cluster. 6. Protagonist names Remi and Tom, both facilitator-supplied.
- (decision) CLICK COLLISION RESOLVED: one click does both - the panel opens on the right with the facts AND the reachable set lights on the map. Selecting is a single gesture; text answer and path answer arrive together.
- (decision) REACHABILITY DILUTION: an adjustable HOP LIMIT controls how far the highlight propagates - one hop, two, all. Default value still to be set.
- (decision) ORPHAN SALIENCE: a discreet counter in the left menu ('3 objects with no stack') that reframes the map onto them on click. The orphan keeps its isolation as the visual signal and the drawing is not modified - so the tool still does not mark it as abnormal, which would be a judgement.
- (event) Creative tool: organic shape study rendered to .working/shape-study-organic-2026-09-10.html. Part 1 shape ladder A-D; Part 2 warmth axis (moot - direction 2 pure was chosen after commissioning); Part 3 live CSS motion specimen at drift +/-3.4px, breathing scale 1.000-1.018 with +/-0.7deg rotation, seven desynchronised periods 9-23s. Adds an anatomy figure solving protrusion attachment and a recognition test at 100/50/25%.
- (assumption) [FINDING from the shape study, bears on the open silhouette question] B's irregular contour does NOT survive zoom-out - at 25% the pebble outlines read as circles again. C's elongation DOES survive. So arbitrary irregularity is a close-range pleasure only, while connection-driven stretch is legible at the default zoom - meaning silhouette can be a real data channel on the landing screen, but only if the deformation is driven by relationships rather than by seed.
- (decision) SHAPE TREATMENT: C - irregular contours PLUS elongation toward linked objects. No neighbour squash. Each bubble has its own pebble silhouette and bulges up to +32% toward every object it links to; a single-link object becomes a teardrop. CLOSES the open silhouette question: the silhouette IS a data channel, driven by relationships, and it survives zoom-out where arbitrary irregularity does not.
- (decision) PROTRUSION ANATOMY solved by the shape study and adopted: an invariant rectangular core the contour may never breach; plates anchored to a fixed BEARING placed in the concavities BETWEEN the stretch bulges, with only the stalk changing length; same-family plates share a baseline so 'pgdata - rw' and 'pg-data - ro' compare character for character.
- (decision) MOTION SPECIFICATION adopted from the shape study: drift +/-3.4px (about 7% of a 48px radius), breathing scale 1.000-1.018 with +/-0.7deg rotation over 5.5-9.7s, seven desynchronised periods (9/11/13/15/17/19/23s) with distinct negative delays so the group never pulses in unison. Applied to the BODY only - core, name, id and pastille rail drift but never deform. Edges anchored 14px inside the bodies with their own +/-1.3px / 21s drift so nothing detaches.
- (assumption) [OPEN, last shape decision] Treatment C's known cost, drawn in the study: without squash, stretched bodies genuinely overlap - api bites 12px into web and 21px into worker. Needs a rule: layout reserves room for the stretch, overlap is allowed and reads as adjacency, or the stretch is capped so overlap cannot occur.
- (decision) OVERLAP RULE: the layout engine knows the coming deformation and spaces bodies accordingly - stretched silhouettes NEVER overlap. The map breathes slightly wider, so slightly fewer objects at equal area, but every silhouette stays fully legible, which the silhouette-as-data-channel decision requires.
- (decision) REVIEWER GATE: Jules opted in to ALL FOUR lenses - rubric walker (mechanical conformance), legibility-at-scale (adversarial, 300-container stress test), brief-fidelity (every departure marked and justified), accessibility (does the floor tell the truth). To run in parallel after DESIGN.md exists.
- (event) Wireframe regenerated and drift corrected: 79 elements, no 'Images' filter anywhere, A TRANCHER box reduced to the single genuinely-open question (default legibility before any filter on a large cluster), Vue noeuds now a real third tab with the distribution purpose. Ready for promotion to wireframes/.
- (event) DESIGN.md distilled. Canonical section order: Brand & Style, Colors (with a measured contrast table), Typography, Layout & Spacing, Elevation & Depth, Shapes, Components (17), Do's and Don'ts, Open Questions. Frontmatter: spec keys colors/typography/rounded/spacing/components plus seven declared extension namespaces (motion, elevation, opacity, stroke, shape, density, layout). 122 hex tokens, every dark token has a -light partner.
- (decision) CONTRAST MEASURED, not asserted: identifiers 15.0 and 18.1; every pastille >=4.2:1 on the body in both modes (health red tightest); every edge >=3:1 over every zone tint in both modes. That floor holds because the six-hue zone rotation is built ISO-LUMINANT, making it a checkable property rather than a coincidence.
- (change) RECONCILIATIONS made at DESIGN.md distillation: (a) direction-2's legend used shape=value (square=container, triangle=volume, hexagon=network) - superseded by the decided shape=family; (b) its published-port chevron dropped as a fifth mark outside the four decided pastille families; (c) attachment edge lifted from #3E5E6B to #5B8494 because the original measured 2.45:1 across zone tints and 'the edges are the product'; (d) the drift-vs-anchor seam (study's +/-3.4px drift against 'the centre does not move by a pixel') resolved as a bounded, zero-mean, non-accumulating rendering excursion inside a reserved cell, layout anchor invariant.
- (assumption) [OPEN, raised by DESIGN.md] 1. Font families - the split, the embedding and the licence constraint are decided; the families are not. IBM Plex Mono / IBM Plex Sans proposed, JetBrains Mono named as substitute, gated on a pgdata/pg-data/pg_data/pgdatal specimen at 10px. 2. Zone hue rotation beyond six networks - the palette rotates over six hues, so a seventh network reuses a tint; the network pastille still answers exactly but two same-tinted adjacent fields is unaddressed. 3. Appearance of the Fit to chart / Reorganise / Isolate / Export / Survey stamp controls - placement fixed, appearance marked [ASSUMPTION] as the direction-2 toolbar chip.
- (decision) FONTS CHOSEN: IBM Plex Mono for identifiers + IBM Plex Sans for chassis. SIL OFL, so AGPL-compatible and embeddable. Designed together so they agree naturally; the Mono's serifed l and slashed 0 make pgdata / pg-data / pgdatal unambiguous - which flow 1's climax depends on.
- (decision) ZONE HUE ROTATION beyond six networks: the rotation simply repeats - a seventh network reuses the first tint. Two same-tinted zones may sit side by side; the NETWORK PASTILLE on each bubble gives the exact answer. This is precisely the role the pastille was given when blurred tint fields were chosen as the default rendering, so the system already covers it.
- (override) CONTRADICTION RESOLVED between the two spines, by Jules's own later choice. His definition of organic included squash against neighbours; when shown the shape ladder he chose TREATMENT C, whose option text read explicitly 'sans l'ecrasement'. The later choice supersedes. DESIGN.md is correct (no neighbour squash); EXPERIENCE.md's Bubble row was stale, written before the ladder. DESIGN.md's rationale: a flat facet between two bodies imitates an edge that does not exist, and squash would make a silhouette depend on its neighbours, destroying the recognition channel.
- (assumption) [TO FIX] Two drifts found at patch time: (1) DESIGN.md's layout inserts a permanent 120px legend band under the canvas, which EXPERIENCE.md's IA chrome inventory ('left menu, bottom tabs, graph canvas everything else') does not list; (2) DESIGN.md uses the word 'legend' for two different things - the left-menu filter swatches and the chart legend band.
- (change) Residual drift patched by hand in both spines: click-collision assumption removed (Jules confirmed one click does both); the adjustable reachability hop-limit written into Interaction Primitives, where it had never landed; four stale Open Question rows deleted (click collision, reachability dilution, silhouette meaning, orphan salience - all decided since); DESIGN.md's stale note about a missing Orphan counter row removed; Export renamed to 'Export (SVG / PNG)' to match EXPERIENCE.md exactly; the chart-legend specimen extended to four strings so it proves the claim Typography actually makes. EXPERIENCE.md Open Questions now: two design questions plus protagonist names.
- (event) REVIEW 1 of 4 - brief fidelity. 16 findings: 1 critical, 4 high, 5 medium, 3 low-medium, 3 low. All four brief-facing overrides are marked and justified in EXPERIENCE.md, but only 2 of 4 in DESIGN.md (export and LOD unmarked there).
- (change) [FIX REQUIRED, CRITICAL] DESIGN.md left menu ships permanent chrome reading 'READ ONLY - SOCKET MOUNTED :RO - NO WRITE POSSIBLE'. Two faults: it silently settles the socket-mediation question the brief expressly reserved for architecture ('read-only at the product level is a promise, not a mechanism'), and it is FALSE - mounting the socket :ro does not restrict the Docker API. Shipping it would display a security guarantee that does not exist.
- (change) [FIX REQUIRED] Attribution error introduced by the facilitator in session and carried into both spines: they assert a contradiction INSIDE the brief between 'legible over impressive' and the screenshot success signal. The brief never links that signal to impressiveness. The tension is real but its source is Jules's own journey 2 ('presque trop pour que ce soit facilement lisible mais tellement classe'), not the brief. Since that invented contradiction is what licenses 'beaucoup a regarder, peu a lire' - which DESIGN.md calls its most important line - the attribution must be corrected without weakening the principle.
- (assumption) [GAP, from brief-fidelity review] Authentication and exposure - the brief's FIRST-RANKED carried question ('who may open the map, and can the socket be mediated') - is absent from both spines entirely, under a blanket completeness claim, while export was promoted to v1 and Flow 2 ends with the protagonist publicly posting his full topology.
- (event) ALL FOUR REVIEWS IN. 83 findings, 8 critical. Rubric 30 (2 crit), legibility-at-scale 16 (3 crit), brief-fidelity 16 (1 crit), accessibility 21 (2 crit). Shape fit rated strong; visual reference coverage rated thin; everything else adequate.
- (override) [FACILITATOR ERROR, must be re-decided] When Jules chose 'the hue rotation repeats, the network pastille settles it', the facilitator asserted the pastille rescues the repetition. FALSE - pastille-network-1..6 uses the SAME six-hue rotation, so it collides identically. Two independent reviewers found this. Past six networks BOTH carriers fail together: neither field nor badge distinguishes network 7 from network 1. Jules decided on a premise he was given wrongly.
- (change) [CRITICAL, from legibility review] The network pastille does not render at rung 0 - it is rung 1. So the landing frame and the exported frame, the ones the product is judged on, carry NO exact answer to 'which container is on which network'. The entire justification for blurred tint fields as the default was that the pastille supplies the exactness.
- (change) [CRITICAL, from legibility review] Node backdrop and network zones compete for the same positional channel. Overlay networks span all nodes by construction, so every zone smears across all six bands and all eleven fields overlap - the mud the design's own zone study predicted at six. Decision-preserving weaker fix available: backdrop as an off-by-default display control.
- (change) [CRITICAL x2, from rubric + accessibility] (1) Identifier channel self-contradicts: the contrast table claims 15.0:1 for the pgdata/pg-data channel Flow 1's climax rests on, while Components->Bubble assigns {colors.ink-3} = 3.42:1 against a declared 7:1 floor; the bubble name has no colour token at all. (2) Node backdrop label #46535C is an untokenised raw hex, the only colour with no light-mode value, measuring 2.49:1 at 10px - and it labels the surface Flow 3 is entirely about.
- (change) [HIGH cluster to fix] Motion drift seam unresolved in the token: {motion.breathe} still carries drift +/-3.4px while the prose says the centre does not move by a pixel; labels travel 6.8px peak-to-peak. 9px type floor violated by its own ramp (4 of 11 roles below it at scale 1.00, three on the exported chart). CVD claim generalised from the health fix to all four families (stack deltaE 3.9, network 2.6). Edge >=3:1 floor measured against ONE tint but mode A composites several (isoline 2.79 at two overlapping fields, attachment edge 2.87 at four). elevation.bubble throws a 21px shadow beyond a body computing to 14-28px at 325 objects, plus an unaccelerated-SVG frame-rate problem.
- (change) [HIGH, functional gap] The brief's VERBATIM filter job - 'show me only what touches backend' - has no mechanism anywhere in either spine. The left menu filters object TYPES; nothing filters by relationship to a named object.
- (change) [HOLES never decided] Whether ordinary tab-focus exists on the chrome ('no keyboard shortcuts, anywhere' under Banned, plus a Don't forbidding glass for focus and no replacement token, reads as mouse-only chrome - far larger than the two exclusions actually decided). Whether type is screen-space or world-space under the LOD ladder - the 9px floor rests on it. Browser/OS zoom never mentioned and actively blocked (1440px display trips off-chart at 110%). Detail panel has no states; Node view has no visual spec; five on-screen controls uncommitted.
- (decision) USER-ADJUSTABLE COLOURS: predefined palettes, all iso-luminant. The user picks between colour sets shipped with the product, each verified to hold the contrast floors. Honours the brief's intent (display control over colours) without letting the user break the guarantee that every edge stays legible over every zone tint - 'the edges are the product'.
- (decision) AMBER DEFINED as running < desired replicas. Docker reports both numbers, so '3/5' is a raw fact rather than a threshold Portolan invented. Removes the unspecified-threshold problem entirely and narrows the brief departure on verdicts - the light now renders facts, with only the green/amber/red framing remaining interpretive.
- (decision) AUTH: Jules - 'On va garder l'auth pour une prochaine version, pour le moment on arrive directement sur l'app.' No login screen in v1; opening Portolan lands straight on the map. This is a v1 scope decision, so the spines state it plainly rather than staying mute - and must note that Flow 2 ends with the protagonist publicly posting a full topology, which makes exposure a live concern even without an auth surface.
- (decision) NETWORK CHANNEL REPAIRED - hue + pattern octave. The hue cycles over six; a FILL PATTERN (solid, half, ring, dotted...) marks the octave, so network 7 is hue 1 with the second pattern. Applied to BOTH the zone field and the network pastille. Infinitely extensible, keeps shape=family intact, and pattern belongs to the cartographic vocabulary. Side effect that closes a separate critical: because the pattern rides on the ZONE too, networks are distinguishable at LOD rung 0, where the pastille does not yet render - so the landing and exported frames regain an exact network answer. Stated cost: a pattern inside a 6px badge is hard and needs proving.
- (decision) NODE BACKDROP: off by default, a display control in the left menu. The overview lands purely relational and the network zones get the whole positional channel back. Resolves the critical finding that overlay networks span all nodes by construction, so every zone smeared across every band. Preserves the landing-surface merge Jules chose, on demand, and reopens no decision.
- (decision) RELATIONAL FILTER - 'KEEP ONLY THIS'. Satisfies the brief's verbatim job 'show me only what touches backend', which had no mechanism anywhere. Click an object, its reachable set lights (existing behaviour), then a 'Keep only this' control promotes that highlight into a real filter that removes the rest. One new gesture, reusing the already-adjustable hop reach, and it honours the rule: highlight dims, filter removes.
- (event) Consolidated validation report rendered: validation-report.html + validation-report.md. 84 raw findings deduplicated to 78 - 7 critical, 18 high, 33 medium, 20 low. Six pairs merged as independent duplicates, each marked with a Converges line naming both lenses: bubble identifier channel; contrast table covering no text roles; the network six-hue rotation 'rescue'; the 9px type floor; motion drift; user-adjustable colours. 10 findings marked resolved-since-review from 8 rulings (8 full, 2 partial).
- (assumption) [NOTE] Report author flagged two count discrepancies in the source reports rather than smoothing them: the accessibility report lists 22 findings, not the 21 its summary claimed (true raw total 84, not 83); the brief-fidelity header said 5 medium / 3 low-medium against its own table's 6 medium / 2 low-medium. Per-finding tables taken as authoritative.
- (event) DESIGN.md fix pass applied: 25 of 26 items (item 25, the stale wireframe drift note, lives in EXPERIENCE.md). Colours 122 -> 142, every one with a -light twin. Components 17 -> 26. Seven new [ASSUMPTION] markers, and the five that existed before (the on-screen controls) are now committed.
- (decision) HONESTY CORRECTION from the fix pass: the octave pattern separates OCTAVES, not hues within an octave. Network 1 vs network 7 is now readable by anyone; network 1 vs network 3 is still deltaE 2.6, the same colour. Zone IDENTITY is carried by the written label at 13.8:1 regardless - that is what actually holds the line. DESIGN.md states this as 'partly repaired' rather than claiming the family is fixed.
- (decision) New rendering rule {opacity.zone-field-cap}: the composited zone field is LUMINANCE-CLAMPED to a single tint. Hue blends, luminance does not accumulate, so the worst realistic composite IS the measured row and the iso-luminant edge guarantee extends to any overlap depth. This is what makes the >=3:1 edge floor a checkable property rather than a claim measured against one tint. Marked [ASSUMPTION] - nobody decided it, but it is the only move that keeps the floor honest.
- (decision) Other structural fixes: type floors split (9px for anything naming a cluster object, 8px for chassis annotation) with the scale clamping at each role's own floor, so the claim holds at 0.90. Elevation now proportional (dy 0.11r, sigma 0.15r) and floored - no shadow below a 30px rendered body, therefore none at rung 0. Pastille rail gap 5.5->3px with shrink-to-fit to an 8px floor and network badges dropping from the right; the old 56.5-into-54.3 overflow is printed as the reason. {colors.focus} is its own token, deliberately not a metal - the metals' one-job rule is load-bearing.
- (event) EXPERIENCE.md fix pass applied: all 18 items. 53 token references, all resolving. 26 of 26 component names matching in both directions. Three residual DESIGN.md staleness items patched by hand (components preamble, Open Questions asymmetry, empty-cluster confidence levelled across both files). Remaining [ASSUMPTION] markers in EXPERIENCE.md: node-region rule 4, chart legend dismissal, screen-space LOD, hover affordance, tab focus, and the two protagonist names.
- (decision) STACK RENDERING - closes the last per-type rendering hole the brief left open, and the one the spines contradicted themselves on. A stack is NOT a bubble and NOT an area: it is a GROUPING OUTLINE girdling the services of one stack, with its name set on it. Two distinct grouping languages coexist: the tinted field says network, the outline says stack. The stack pastille remains the exact answer where the two cross. Rejected: stack-as-bubble, because fifteen services converging on one stack node recreates exactly the fan-in that got images removed from the map.
- (decision) EXPORT 'SAFE TO SHARE' MODE: a checkbox at export masks IPs and CIDRs while keeping the whole structure - shapes, zones, edges, object names. The screenshot stays impressive and stops being an infrastructure plan. Answers the brief's warning that an exposed full topology is 'a reconnaissance gift', which mattered because Flow 2's climax is literally posting one publicly.
- (decision) REFRESH INTERVAL: a control in the left menu, joining the other display settings. Honours the brief's 'auto-refresh every 5-10s, configurable' literally, and lets someone on a large cluster slow the survey down if it costs. Previously both spines repeated the word 'configurable' from the brief and offered no surface.
- (decision) KEYBOARD FOCUS RULED: ordinary tab focus on chrome controls EXISTS, with a visible focus ring. What is excluded is keyboard TRAVERSAL of the graph and keyboard SHORTCUTS. This is Jules's ruling, not the facilitator's reading - the [ASSUMPTION] marker comes off.
- (decision) STACK OUTLINE POSITION IS DERIVED - the trap in the stack decision, caught at application. The outline follows where the zones placed its members and never asks the layout for position, because TWO MARKS OWNING POSITION is the node-backdrop failure arriving through a second door. Consequence stated: the hull threads around non-members where it can and encloses them where it cannot - exactly the case the stack pastille exists to settle.
- (decision) Stack outline is deliberately NOT given the six-hue stack rotation - that would put twelve colours on one canvas, widen a family already at deltaE 3.9, and make the two grouping languages harder to separate. One graphite stroke; the diamond pastille stays the exact answer. Zone-vs-outline distinguishability specified across seven axes: field-with-no-boundary vs boundary-with-no-field, open fading coastline vs closed drafted offset, overlaps-by-construction vs partitions, label inside the field uppercase and tracked vs label on the stroke as-typed, owns position vs derives position.
- (decision) SAFE TO SHARE is ON by default [ASSUMPTION]. Masked values render as a run of blocks at the mono's character pitch so nothing reflows. What survives masking and is still identifying: object names, stack names, and the topology shape itself - stated plainly rather than implying the export is anonymous.
- (event) Four closing decisions applied to both spines. Token references 85->95 (53->60 distinct), zero unresolved. Components 26->28, matching in both directions. The 'stack' object-type pastille value and its two colour tokens were deleted exactly as 'image' was. New typography role stack-label (twelfth). Four contrast rows added, two amended.
- (assumption) [PENDING] Key-screen mocks were commissioned before the stack-outline decision, so they will not show it. Decide on arrival whether to re-render or annotate - the overview is precisely where the outline is most visible.
- (event) Key screens rendered and verified 1:1 in headless Chromium: .working/key-overview.html (whole cluster at rung 0, purely relational, orphan floating) and .working/key-overview-selected.html (api selected: panel open, reachable set lit at one hop, rest dimmed to 0.18, 'Ne garder que ceci' offered, pgdata-rw and pg-data-ro side by side on one brass-ticked baseline). No external requests, no JS.
- (decision) Composition decisions the mocks had to take and the spines did not specify, all recorded in-file: the detail-panel column is ALWAYS RESERVED (884px canvas in both frames) because letting the canvas widen on a click would re-lay the map, which Layout stability forbids; octave I's pattern IS solid, so a three-network cluster renders plain fields and the channel is decoded in the legend rather than faked; the composite luminance clamp rendered as mix-blend-mode lighten in an isolated group; attachment-edge colour resolved toward {colors.edge-attach} because that is the token the contrast table measures, making the floor checkable.
- (change) MOCK CONFLICT SURFACED: DESIGN.md carries two contradicting rules for the attachment edge - stroke.edge.attach says {colors.edge-attach}, components.edge.attach-rendering says 'in the zone's own hue'. The mock chose edge-attach on the grounds that it is the measured token. The spine still contradicts itself and must be reconciled.
- (change) Attachment-edge contradiction fixed by hand in DESIGN.md line 518. components.edge.attach-rendering said the stub renders 'in the zone's own hue', contradicting stroke.edge.attach, the contrast table and the Edge component, which all say {colors.edge-attach}. Resolved toward edge-attach - it is the token the contrast table measures at 4.2-4.5:1, so it is the only one that makes the floor checkable. Surfaced by the key-screen renderer, which hit the conflict while rendering.
- (event) Independent verification by the facilitator (not a subagent): both frontmatters parse as YAML; DESIGN.md carries 142 colours and 28 components; EXPERIENCE.md 60 distinct token references, 0 unresolved; DESIGN.md 126 distinct, 0 unresolved. The single reported miss is the prose placeholder {token} at DESIGN.md:624, which explains the convention rather than referencing anything.
- (assumption) [FINDING, discovered by rendering the stack outline] NEAR-CONCENTRIC CONTOURS. A stack and a network that share members are drawn around the same bodies, so their contours run near-concentric. Measured in the key-overview mock: 455px of the obs outline's 1140px perimeter runs within 14px of the monitoring isoline; 346px of shop's 1961px runs within 14px of frontend's. The seven distinguishability axes separate the two grouping languages in KIND but not when they coincide in POSITION - which is the direct consequence of the outline deriving its position from where the zones placed its members. Belongs in both spines as a stated cost.
- (assumption) [FINDING] Zone labels can be left with nowhere to go. An exhaustive 2px sweep of each field: BACKEND had room (moved 18px, clearance 6.6 -> 23.8px); FRONTEND has none (best clearance 4.9px, 145px away - the outline cuts the field into free bands 70-100px wide against a 132px CIDR); MONITORING has none (best in the whole field 0.8px against the 0.9px where it stands). Both left in place, survivable only because the layer order puts the stroke behind the label and {colors.ink} reads 13.8:1 against the outline's 4.6:1. The zone owns position and the outline derives it, so neither moves - the label, a free annotation inside its field, is the only thing that can.
- (event) Key-screen mocks updated with the stack outline. Construction: per-member convex hull, members sewn by the convex hull of each pair in the minimum spanning tree of cell-surface distances, union dilated by 16px (8px reservation + 8px clearance), so the fillet radius IS the clearance. Derived position forced NO non-member enclosure on this cluster - prometheus clears the shop outline by 92.6px, the orphan by 109.2px, api clears obs by 138.7px. Nothing was smoothed to achieve that; had the geometry enclosed a body it would have stayed enclosed.
- (assumption) [MOCK DEFECT, low] The Networks filter row reads 'frontend 1' while two services sit in the frontend field (web and api). Pre-existing in the mock, not a spine error.
- (event) Artifacts promoted and linked inline. mockups/: key-overview, key-overview-selected, direction-2-instrument-de-precision, shape-study-organic, zone-overlap-options. wireframes/: ia-portolan. .working/ keeps the three rejected directions as audit trail. Ten inline links inserted at the sections each artifact illustrates; spine-wins-on-conflict stated once per file; no .working/ reference remains in either spine.
- (decision) Near-concentric contours recorded in both spines with the measured figures, and added as a third open question in DESIGN.md / a new row in EXPERIENCE.md: 'Do the two grouping contours stay apart where they coincide?' DESIGN.md states flatly that the seven distinguishability tells separate the two languages in kind and NONE OF THEM IS A DISTANCE.
- (change) Flagged at both shape-study links: the study's motion specimen animates a POSITIONAL +/-3.4px drift, which both spines have since retired in favour of outline excursion. The mock is not authoritative on motion, and now says so.
- (event) doc_standards applied: skill:bmad-review lenses=structure,prose, run over both spines. Four lens runs. Both structure lenses independently reached the same diagnosis: the documents are REFERENCE/DATABASE in model (an implementer jumps to the component they are about to build) but were AUTHORED as argument - rationale, rejected alternatives and rebuttals of their own earlier drafts woven into the binding rules. DESIGN.md: 18 recommendations, ~770 words of self-correction prose off the reading path. EXPERIENCE.md: 15 recommendations, ~505 words of revision residue.
- (change) PROSE FINDING, highest risk, EXPERIENCE.md Export: 'masks every IP and every CIDR in the exported frame and nothing else: the zone sublabel..., any address on a plate, any address in the legend.' The colon reads as introducing what is NOT masked - the list says the opposite of what it appears to say, on the sentence governing what ships in a public screenshot.
- (change) PROSE FINDING, factual, DESIGN.md Labelled protrusion: 'Text at 15:1 - the highest contrast requirement in the file' is wrong three ways. 15:1 is a MEASURED ratio not a requirement; that row's floor is 7:1; and three other rows measure higher. The sentence contradicted its own table's Floor column.
- (decision) SPELLING REGISTER: both spines are en-GB throughout (colour, behaviour, centred, and a control literally named Reorganise) against the Microsoft Writing Style Guide's US English. Resolved by DECLARING en-GB as a deliberate exception rather than converting - converting would rename a UI control and fight a settled voice. One genuine inconsistency fixed each way: artifacts -> artefacts in DESIGN.md, behavioral -> behavioural in EXPERIENCE.md.
- (decision) Both structure lenses independently PRESERVED the mock-is-stale warnings, which pattern-match the revision residue they cut elsewhere. Rationale, in their words: those warnings stop a builder copying geometry and a legend key out of a superseded artifact the document itself links to. Not history - an active guard.
- (event) Editorial pass applied to both spines. DESIGN.md: all 12 structure + 23 prose findings, body 14929 -> 15995 words (+7.1%) because the self-correction strand was MOVED to a Revision Notes appendix rather than deleted, plus a TOC and a scope note as net additions; ~770 words leave the reading path. EXPERIENCE.md: all 15 structure + 24 prose findings, 13393 -> 13145 (-1.9%); Component Patterns split into Graph marks / Chrome controls / Chart apparatus with the seven longest cells promoted to subsections; bold saturation cut (Stack outline 15 spans -> 8, Export 15 -> 7).
- (event) SPINES FINALIZED. Both set status: final, updated: 2026-09-11. Final verification by the facilitator: DESIGN.md 142 colours, 28 components, 130 distinct token references, 0 unresolved; EXPERIENCE.md 59 distinct references, 0 unresolved; 28 component names matching 1:1 in both directions; three Key Flows with five climax beats intact; 12 [ASSUMPTION] and 6 [DEPARTS FROM BRIEF] markers preserved.
